# 模块③ · 结构与口径（SPEC）

> 这一份回答「**这个仓怎么组织、什么不许动、数据是什么、一个单元要做到什么程度、好不好看哪个数**」。
> 由旧 `SPEC.md` + `SPEC.md` + `SPEC.md` 三份合并（2026-09-20），**章节号原样保留**。

> **本文件包含**：§0 目录结构 · §1 硬约束 · §2 防泄漏铁律 · §3 数据层 · §4 切分口径 · §7 评估口径
> ｜ **文档入口**：[`README.md`](README.md)
> ★ 统一交付格式与版本范围要求写在 [`README.md`](README.md) §2 / §3，本节不重复。
> ★ **文中凡提到 `mx/*`、`main.py`、`run.py`、`tools/*`、`promote` 的地方，都是重构前的东西** ——
> 那些文件已全部删除，保留它们只为解释"当初为什么这么定 / 踩了什么坑"。
> 现役实现只有两份脚本：**`V16/model.py`**（配方 / 数据 / 切分 / 模型 / 损失 / 训练）+
> **`V16/analysis.py`**（推演拼接 / IC / 可执行收益 / 现金回测 / 报告 / 推荐表）。
> ★ 章节号（`§N`）是**全库统一**的：指向别的文件时会写成「文件名.md §N」并带链接。

---

## 0. 目录结构（2026-09-20 重构后）

```
model/
├── LEADERBOARD.md       ← 模型天梯榜（一行一版，4 个指标；2026-09-20 由 `README.md` 改名）
├── README/              ← ★ 全部文档（本文件在其中，共 4 份）
├── preparingdata.py     ← ★ 初加工：上游因子 → trainingdata/（**单文件独立脚本**，唯一写入口）
├── trainingdata/        ← 初加工产物（模型只读）：factors / target / amount / fac_sample / prices / log/ + meta.json
├── V16/                 ← ★ 模板单元（自包含：model.py + analysis.py + train.sh）
├── best/                ← 实战单元（★ **当前为空**）
└── history_iterations/  ← V1 ~ V15 归档（**不要跑**）+ 旧文档归档
```

★ **顶层只放这几样**：`preparingdata.py`（构建期唯一入口）· `trainingdata/`（产物）·
`V16/`（模板单元）· `best/`（实战单元）· `history_iterations/`（归档）· `README/`（文档），
外加一个 `LEADERBOARD.md`（排行榜）。原先的 `mx/`（引擎）、`main.py`、`tools/`、`tests/`、`logs/`、
`state/`、`pics/`、`requirements.txt` **已全部删除**。

★ **文档只剩 4 份**，入口是 [`README.md`](README.md)（迭代规程）：

| 文件 | 装什么 | 章节 |
|:--|:--|:--|
| [`README.md`](README.md) | **入口 + 规程**：版本迭代规则 · 时间口径 · 交付格式 · 命令 · 产物位置 | —— |
| `SPEC.md`（本文件） | **规矩**：目录 / 硬约束 / 防泄漏 / 数据层 / 切分 / 评价口径 | §0~§4 · §7 |
| [`LESSONS.md`](LESSONS.md) | **结论**：核心规律 `R*`/`U*`、已确认事实、死胡同、坑 | §8~§11 |
| [`TODO.md`](TODO.md) | **待办**：操作纪律 + 队列 + 研究方向 `RD*` | §12 |

★ **章节号 `§N` 全库统一**，所以 `§8.5`、`§11-24` 这类引用在任何一份文件里都按同一张表解析；
跨文件引用写成「文件名.md §N」。**§5 / §6 / §6.5 已随旧 `mx/` 引擎副本一并作废**（编号不回收）。
（2026-09-20 之前这些内容挤在模块根的 `README.md` + `LOG.md` 两份里；当天拆成 9 份，
随后按用户要求收敛为 4 份 —— 被并掉的 `SPEC.md` / `SPEC.md` / `LESSONS.md` / `LESSONS.md`
原文归档在 `history_iterations/docs_archive_20260920/`。）

## 1. ★★★ 硬约束（拍板过的，别改）

| # | 约束 | 含义 |
|:--|:--|:--|
| 1 | **下游任务是 A 股日横断面回归排序** | 每天给全市场主板股票打分排序；不是时序点预测、不是分类涨跌 |
| 2 | **只主板、非 ST** | 与模块② **逐格一致**：股票池掩码在初加工阶段由模块② 的 Engine 生成并落盘 |
| 3 | **一个全局 Python 环境** | `/autodl-fs/data/miniconda3/bin/python`；装库必须 `sudo …/pip install`，**不要装 `~/.local`**（换机器就丢） |
| 4 | **可交易性/组合/成本归模块③** | 用户 2026-09-15 裁定：「停牌/涨跌停/流动性属于模型策略」 |
| 5 | **CPU 先跑通、再原样转 GPU** | 代码里**禁止硬编码设备**；由 `train.device` + `torch.cuda.is_available()` 决定 |
| 6 | **产物与因子侧同契约** | 4 列 `trade_date/stock_code/value/rank`（string/string/float32/float32），年分区、原子写 |
| 7 | **改任何东西都开新版本** | 单元只增不改；引擎改了要 `freeze` 刷新并记进版本 README |

---

## 2. ★★ 防泄漏铁律

**铁律一：测量工具（test 窗）不得被透视 —— 其余切分方式一律不预设。**
标签是前视的（`label_ret_hd(T) = open(T+1+h)/open(T+1) − 1`），相邻交易日样本**共享未来 h 天行情**。
所以**测试窗**两侧各让开至少 `h` 个交易日：**purge**（切点前的样本标签伸进了 test 窗）+
**embargo**（切点后的样本标签回看 test 窗之前）。**这是唯一的硬约束** ——
不许任何训练/验证样本的标签伸进 test 窗。
这条守的是**测量工具本身** —— 一旦 test 窗被透视，"被 test 惩罚"这个纠错机制就失效，
所有版本比较都失去意义。落点：`V16/model.py::splits()` 的 `cutoff = first − (h+1)`；
`delta` 必须按**交易日**、按实际用到的标签的 `max(h)+1` 取值（两条实测反例见 [TODO.md](TODO.md) RD2）。

★ **test 窗之前的切分方式一律不预设**（用户 2026-09-20）：
train 与 valid 之间要不要隔离、隔多久、时间序还是随机、验证集多长/在哪/什么形状 ——
**都只是可研究的选择**。它们最多让训练过程过拟合，而这**会被 test 集惩罚**，
所以不需要固定限制死。现行口径（全历史等分 4 段 + 两侧各 6 交易日）只是一个 baseline，不是规范。
实验网格与实测数字见 [TODO.md](TODO.md) **RD2**。

**铁律二：一切统计量只用当日截面。**
不许出现全样本 `mean/std/quantile`（标准化、填充、缩尾都不行）—— 那是未来信息。落点：
特征用 `rank`（初加工已做成当日截面百分位）；线性模型缺失填**固定常数 0.5**（不用当日中位数）；
**集成是四折打分直接相加**（★ 2026-09-20 用户改：**不做任何归一化 / z 标准化**）；
打分是**逐行**的（由 `V16/analysis.py --audit` 的**逐日截断 + 重载推理对拍**守这条）。
新增的标签变换与损失（`V16/model.py`）同样遵守，且逐条在函数注释里写明理由。

**★ 铁律三：一切 top_ret 口径必须套"买得进"掩码，且掩码必须对齐到【成交日】。**
（2026-09-18，两条血的教训，第二条更贵）

不看可执行性的 top_ret 会**奖励"挑明天一字涨停"的模型**：模型专挑"T 日收盘涨停"的票
（占 top1 的 35.3%），这类票 T+1 开盘大概率一字、**买不进**。实测 V3 折 1
（`gbdt_w/5d`，253 天）：不套掩码 Σtop1 = **+3.777**，套上**对齐正确**的掩码 = **−1.576**
（复利 −87%）。

★★ **而且"套了掩码"还不够 —— 掩码必须对齐到成交日 T+1，不是信号日 T。**
`analyze` 原先传的是 `entry_ok[T]`（= "昨天能不能买"），恰好放过了模型专挑的那批票，
于是主判据**系统性偏乐观约 2.2 个 Σ 单位**（+0.637 vs 正确的 −1.576）。
**这类错位不报错、不崩、不看两套口径对不上就发现不了** —— 详见 [LESSONS.md](LESSONS.md) §8.5。

这不是交易摩擦（费用/滑点），是**可执行性**，属于硬约束 #4（可交易性归模块③）。
落点：`V16/analysis.py::costfree(pred, panel, prices, days, n)`；买得进掩码取自 `prices` 块、
**对齐到成交日 T+1**，默认路径一律套真实可买掩码，**不许提供"不滤"的开关**。
★ 2026-09-20 用户拍板：**拿不到价格层就硬失败，不许静默降级**（原来的"打一行 ⚠️ 并标
`exec_filter=False` 继续跑"是被否掉的写法 —— 见 [SPEC.md](SPEC.md) §7）。

---

## 3. 数据层：`trainingdata/`（初加工产物）

**四块产物，按年份分区**（2026-09-20 **下午**由七块收敛而来）：

```
trainingdata/
├── meta.json                            清单 / 边界 / 上游指纹 / 逐年摘要 / 覆盖率 / 各块指纹
├── factors/year=YYYY/data.parquet       trade_date, stock_code, <337 个特征>   ← 全部因子
├── target/year=YYYY/data.parquet        trade_date, stock_code, <5 个 target>  ← 全部收益
├── amount/year=YYYY/data.parquet        trade_date, stock_code, amount         ← 每日成交额（元）
└── fac_sample/year=YYYY/data.parquet    trade_date, stock_code, <20 个抽样特征>
```

| 块 | 装什么 | 谁用 | 动它会改 `panel_digest` 吗 |
|:--|:--|:--|:--|
| `factors` | 全部特征的**当日截面 rank**，方向已统一成"越大越好" | 训练/评价 | **会** |
| `target` | **1d / 3d / 5d / 10d / 20d 五个 target**（`label_ret_*d`）的收益原值 | 训练/评价 | **会** |
| `amount` | 每日**成交额（元）**，用户口径叫"市场所有的流动性" | 回测/策略 | 不会 |
| `fac_sample` | `factors` 里**随机抽 20 列**（seed=42），值逐字取自 `factors` | 冒烟/最小输入 | 不会 |

### 2026-09-20 下午删掉的三块，以及为什么

| 删掉的 | 为什么 |
|:--|:--|
| `universe` | **实测 4,475,340 行里 `in_universe=False` 0 个**。轴就是那 2115 只冻结池（`build_axis` 取 `Engine.codes`），掩码是同一批代码过上游 `universe_for`，而冻结口径（主板 ∩ 至今存续 ∩ 从未ST ∩ 上市≤2018-01-01）保证它必然全 True —— **恒等掩码，没有信息**。顺带发现它以前还是**白算**的：掩码根本没进 `_coverage`（那只吃 `factors`），算完只被原样写盘 |
| `P` | 价格/成交量**原料**（81 MB）。不在布局里；★ **构建它的代码也于同日删掉了**，回测要用得先把它加回来 |
| `sample/` | 同构小样本树（500 只 × 近 2 年，128 MB）。**构建代码同样已删** |

★ 删 `P` 之后 `amount` 反而更重要：`amount` 与 `P.amount` 实测**逐位相同**（9 年逐年核对），
所以删 `P` 之前它是冗余列，删之后它是成交额的**唯一**存放处。

★ **`panel_digest` 只哈希 `columns.features` + `columns.labels` + 逐年 `files.factors.sha`。**
哈希串里**不含块名**（块名只用来查 key），所以**块改名本身不换锚点** —— 但前提是
`preparingdata.py` 的块名常量与 `meta.json` 的 key **同步**改；只改一头会**静默换掉锚点**
（实测：漏改会得到 `d90b20cf37710f94`，正确值是 `798ccb32214853a8`）。
于是 `amount` / `fac_sample` 是**只增不改**的 —— 补建它们不会作废任何结论。
反过来，`factors` / `target` 一变，锚在这个指纹上的结论**全部要重跑** ——
★ 注意**改 `columns.labels` 的顺序也算变**（那个列表直接进哈希），所以别顺手"整理列序"。
这条界线要守住。

★ **`fac_sample` 同时登记进 `meta.feature_sets["sample_20"]`** ⇒ 单元只写
`DATA = {"features": "sample_20"}` 就能对 `factors` 做 20 列的快速冒烟，
**引擎完全不需要知道 `fac_sample` 这个物理块的存在**。
（`sample_20` 是**特征集名**不是块名，所以这次块改名没跟着改。）

### 三个口径（都是踩过坑才定的）

1. **权威网格 = 交易日历 ∩ [上游产物实际最早/最晚日] × Engine.codes，缺格写 NaN**，覆盖率进 meta。
   实测上游几百个因子**并非逐格一致**（晚起点因子早年缺天、末尾参差）——
   "不一致就报错"的老写法在放开全历史时必然误报，所以改成"对齐 + 覆盖率报告"。
   ★ 端点一律取**产物**的实际日期，**绝不用交易日历**（日历含未来 16 个交易日）。
2. **`factors` 存 `rank` 列并把方向烘焙进来**：`higher_is_better=False` 的因子存 `1 − rank`，其余存 `rank`。
   语义统一成"越大越好"，值域仍是 [0,1]。**`target` 存标签 `value` 原值，不翻方向。**
   ★ 为什么不是 `−rank`：旧写法把翻转列变成 [−1,0]，再被 `clip(0.001,0.999)` 一夹就**整列压成常数**
   —— V1 时代 83 个因子（37%）就是这样被抹平的（见 [LESSONS.md](LESSONS.md) §8）。`1−rank` 恒在 [0,1]，不可能再出这种事，
   而且 0.5 在两种表示下都是缺失的中性填充值。
3. **`meta.json` 最后写**：中途失败时下次能从旧 meta 正确重算，不会把"半新半旧"当已完成。

★ **`P` 若重建，存的仍是"原料"而不是算好的复权价**：上游 `hfq_X = ffill(X) × ffill(adj_factor)`
是逐点乘积，而 `ffill` 的效果**取决于作用窗口** —— 若在导出时就把 `hfq_*` 算死，等于把"导出窗口"
焊进数据，消费端换一个读取窗口首行就对不上了。存原料、由消费端在**它自己的窗口内**套用同一套
运算，才逐位保真。★ `P` 已于 2026-09-20 从布局里删掉、构建代码也删了（见上文）；
这条口径留给将来若要把价格层加回来时参考。

### 增量更新（`preparingdata.py`）

- 窗口 = `[上次末日 − lookback, 上游最新日]`，**只重写窗口覆盖到的年份分区**，未触及的年份零 I/O；
  "上游没有新东西"走零 I/O 快路径直接跳过。
- `lookback` 默认 = **最长标签的 h + 1 + 冗余 7**（= **28 个交易日**，2026-09-20 瘦身时定的口径）。
  ★ `h + 1` 是**硬下限**：标签 `T` 用到 `open(T+1+h)`，要等 h+1 个交易日才定型；窗口比这短，
  那些天一旦出窗就**再也不会被重算**，20d 标签会永久停在 NaN。
  ★ `+7` 是**冗余覆盖**（用户要的）：即使标签已定型，也把那 7 个交易日重算一遍，吃掉上游的小幅回改。
  ★ 为什么**不**写成"每个标签各自刷最新 7 个有效行"：跨度大时会漏 —— 若上次跑在 30 个交易日之前，
  20d 标签是在 `T_max−51 ~ T_max−21` 里陆续定型的，只刷最新 7 行就漏掉 `T_max−51 ~ T_max−29`。
  `上次末日 − lookback` 这种写法对任何间隔都成立。
- 上游**回填历史**（新增年份分区）由指纹里的 `per_factor_years` 发现 → 那些年份整年重建。
- 增量只重算**窗口覆盖到的年份**，未触及的年份零 I/O；窗口外的行从既有分区读回来原样拼在前面
  （`_load_keep`），所以增量与全量的产物**逐字节相同**（2026-09-20 实测过）。
- ★ **`P` 与小样本树的构建代码已随瘦身删掉**，不会再自动长回来 —— 它们原先挂在
  `prepare()` 的两条路径上，跑一次不带参数的 `preparingdata.py` 就会把 81 MB / 128 MB
  悄悄恢复，与四块布局矛盾且不报错。

### 命令

```bash
PY=/autodl-fs/data/miniconda3/bin/python        # ★ 必须全路径
cd /autodl-fs/data/model

$PY preparingdata.py                 # 自动：没有产物→全量；有→增量（日常就这一条）
$PY preparingdata.py --full          # 全量重建
$PY preparingdata.py --years 2025 2026             # 只加工指定年份
$PY preparingdata.py --check         # 只校验：meta↔文件↔上游 + 取值域抽查（日常第一条）
$PY preparingdata.py --amount-only   # ★ 只补 amount（每日成交额），不改 panel_digest
$PY preparingdata.py --fac-sample-only     # ★ 只补 fac_sample（因子抽样），同上
$PY preparingdata.py --meta-only [--freeze]  # 只补 meta 派生字段（不读因子侧、不碰数据文件）
$PY preparingdata.py --jobs 8        # 读并发（默认 6；共享盘上别开大）
$PY preparingdata.py --out trainingdata_new --full   # 写到另一份目录（现有产物一字不动）
$PY preparingdata.py --features-from <meta.json>     # 锁定特征清单（与旧产物逐位对拍用）
```

> ★ **日常就一条命令**：`$PY preparingdata.py` —— 有上游新数据就走增量、没有就零 I/O 跳过。
> 增量窗口 = 上次末日往前 **28 个交易日**（= 最长标签 h + 1 + 冗余 7，见模块 docstring）。
>
> ★ 快照**当前不是冻结状态**（2026-09-20 瘦身时解冻，否则日常命令会被闸门拦住）。
> 要重新上锁 / 解锁：`--meta-only --freeze` / `--meta-only --unfreeze`（**只翻标记，
> 不重建、不换指纹**）。

★ **`preparingdata.py` 是单文件独立脚本**，不 import 项目内任何模块，只依赖
numpy / pandas / pyarrow + 上游 `fea`。换机器交接时，一个文件就是全部。
上游路径可用 `MX_UPSTREAM` / `MX_FACTORS_DIR` / `MX_FACTORS_STATE` / `MX_RAW_DATA` 覆盖。

### ★ 换数据（快照）的正确姿势

**另建 → 体检 → 换，别原地覆盖**：

```bash
$PY preparingdata.py --full --out trainingdata_new     # 另建，现有产物一字不动
$PY preparingdata.py --check --out trainingdata_new    # 先体检（支持指定目录）
mv trainingdata trainingdata_bak_<日期> && mv trainingdata_new trainingdata
```

★ 为什么不原地覆盖：所有已出结论都锚在旧 `panel_digest` 上，覆盖掉就**没有对照组**了。
代价只是几 GB 磁盘（同盘 `mv` 瞬时）。

★ 冻结闸门**不挡这条**：`--out <新目录>` 那个目录还没有 `meta.json`，闸门读不到锚点就放行
（闸门只拦"往已有快照上重建"）。所以冻结期间依然可以另建再换。

### ★ 上游会变，而且不一定会通知你（2026-09-20 血的教训）

**上游因子工程在 2026-09-19 之后被整体重算过，下游没有收到任何通知。** 实测到的变化：

| | 旧快照（2026-09-19 建） | 上游现在 |
|:--|:--|:--|
| 覆盖 | 2012-01-04 ~ 2026-09-18 | **2018-01-02** ~ 2026-09-18（2012~2017 分区被删） |
| 因子数 | 306 个特征 | **337** 个特征 |
| 台账文件 | `log0918_post/dayhash.tsv` | 目录改名成 `logs/`，该文件不存在 |

**而且变的不只是范围**：拿 2026 年逐格比对，306 个特征里 **265 个逐位相同、41 个有差异**，
差异的**全部是财报类因子**（`asset_growth_qoq` / `rd_intensity` / `yoy_revenue` / `net_margin_ttm` …），
每个覆盖约 36.76 万格、平均绝对差 0.0005 ~ 0.0017 —— 这是**财报数据被厂商重述后整段回填**的特征。

⇒ **任何用旧数据训练出来的模型，它的基本面输入已经过期。**

**差点造成事故的一条路径**：在旧快照上跑一次不带参数的 `python preparingdata.py`（日常命令）
会这样走 —— 上游指纹变了 → 不是快路径 → 31 个新因子在 2018~2026 都有分区 ⇒ 这 9 年**整年重建**
（用新的 337 列），而 2012~2017 仍留 306 列 ⇒ 得到一个**半新半旧的嵌合体**
（`factors` 分区列数不一致、meta 声称 337 列但轴只剩 2018 起），**而且不报错**。

**处置**：先把旧快照显式冻结（`--meta-only --freeze`，锚点 + 原因写进 meta），
任何重建路径当场被拒；然后按用户裁定**整体重建**并对齐上游。
`meta.json` 的字节备份留在 `/tmp/trainingdata_meta_pre_refactor_20260920.json`。

**教训**：`--check` **抓不到"上游丢了一批历史数据"** —— 它只比对"上游最新日 / 最早日"
与产物的边界，上游把早年分区删掉时它认为"上游变晚了"而不是"出问题了"。
所以**换机器或长时间没跑之后，先看 `--check` 输出里的"上游扫描：N 个因子 · 分区年 M 个"
与 meta 里记录的数目对不对得上**，再决定要不要重建。

### 现在还会不会现读上游？

**训练 / 评价 / 回测全链路都不碰上游**，只读 `trainingdata/`（四块 + `prices`）——
这是模块③ 唯一的数据来源。会现读上游的只有 `preparingdata.py` 自己（构建与 `--check` 对账），
所以**引用上游统计量的结论要标注"上游可能已变"**。

★ 拿不到 `prices` 时该**硬失败**（宁可崩掉，也不要像当年那样被 `except` 静默降级成
"不套可买掩码"、给出看着正常的错数字 —— 见 [LESSONS.md](LESSONS.md) §11）。

### ⚠️ 2026-09-18 深夜：因子侧冻结股票池 → 回测路径静默错位（机理留存）

> **2026-09-20 补注（晚更新）**：本节那道闸门（`mx/upstream.py::assert_price_axis`）随顶层 `mx/`
> 一起删除了；价格层也换了载体 —— 现在是 `trainingdata/prices` 块，由
> `preparingdata.py::build_year_prices()` **按 `(trade_date, stock_code)` 键 `reindex` 落到面板格子**上，
> **不按列号**，所以"甲的价格写进乙的列"这种错位在当前链路里没有载体。
> 保留下面的记录是因为**「面板代码轴 ≠ 上游引擎轴 ⇒ 静默错位」这条机理是通用的**：
> 任何**重新**按位置/按轴对齐价格的方案，都必须把这道校验一起带回来。

因子侧按用户拍板把股票池从「主板全集 + 动态进出」3485 只改成**固定名单 2115 只**
（主板 ∩ 至今存续 ∩ 从未ST ∩ 上市 ≤ 2018-01-01；**代价是幸存者偏差，用户已确认接受**）。
`Engine.codes` 已经变成 2115 —— 而本模块的快照仍是旧口径的 **3484 只**。

**后果（实测，不是推测）**：`fea/prices.py::_place_from` 把价格按「引擎位置」落格，再由
`Panel.place` 按**列号**写进调用方的面板 —— **只要面板的代码轴与 `Engine.codes` 不一致
（集合或顺序任一不同），价格就会静默错位**：甲的价写进乙的列，不报错、不出 NaN。
实测旧快照：列 2 的 `000003.SZ` 拿到 `000006.SZ` 的价、列 1000 的 `002377.SZ` 拿到 `002911.SZ` 的价。
⇒ **在旧快照上跑的任何策略/回测结论都是错的**（IC 层不碰价格层，不受影响）。

**当时的闸门**（已随 `mx/` 删除，这里只记它做了什么）：`assert_price_axis(cfg, codes)` 在
`strategy.price_ctx` 与 `backtest._price_arrays` 进入价格层之前硬校验（两向都验过：旧轴被拦、引擎轴通过）。
报错信息里写了三条出路：

1. 只看 IC 层 → `$PY analysis.py --no-backtest`（训练/IC 完全不碰价格层）；
2. 在新口径下重建快照 → `preparingdata.py --full --unfreeze`（面板指纹会变，旧结论全部重跑）；
3. 要保住旧快照的回测 → 需要让价格层支持**任意代码表**（`PriceLayer(up, cfg, cal, codes)`
   本身就支持，只是引擎只传自己的），或把 `price_ctx` 改成"按引擎轴算、再按面板代码 gather"
   （内存有界，约 60 MB/字段）。**这条路要不要走，等用户拍板。**

★ 顺带的发现：小样本树（500 只）跑回测**本来就**是错的（同一类错位，只是以前没人发现）。
现在会被闸门挡住 —— 冒烟要看回测请用全快照，或加 `--no-backtest`。

### ★ 新旧快照**不可直接比**（幸存者偏差，2026-09-19 起）

> **2026-09-20 补注**：本节讲的那两份旧数据（3484 池归档与 306 特征新池快照）**都已删除** ——
> 用户裁定"数据只需要对齐上游，上游删了这里也没必要留"。保留下面的推理是因为
> **「跨池/跨特征集不可比」这条纪律永远成立**，而且**2026-09-20 又发生了一次同类事件**
> （上游重算因子 ⇒ 特征集与年份范围都变 ⇒ 旧结论再次全体作废，见 `LESSONS.md`）。
> ★ 教训升级：不只是"跨池不可比"，而是**"任何一次上游重算都会让下游结论作废"** ——
> 所以每次引用旧结论前，先核对它建在哪个 `panel_digest` 上。

因子侧 2026-09-19 完成口径切换：股票池 = **固定名单 2115 只**（主板 ∩ 至今存续 ∩ 从未ST ∩
上市 ≤ 2018-01-01），全量重算后 **2,160,796,095 行**、零池外残留、`增量==全量` 复测 0 不一致。
旧快照（3484 池，含退市与曾 ST 股）保留为归档对照组。

⇒ **跨这次切换的净值/回测数字一律不可比**：新口径要求"至今存续"，等于事前剔除了后来退市/长期
ST 的股票，**绝对收益被系统性抬高**（用户已确认接受这个代价）。要比较，只能在**同池内部**比
（旧归档 vs 旧归档、新快照 vs 新快照），或明确标注"跨池不可比"。
IC 层同理会受影响（截面变了、排名变了），但**方向性结论**通常比绝对水平稳，引用时仍要注明池子。

### ★ 2026-09-20 下午：块改名（`X`/`Y`/`sample_input` → `factors`/`target`/`fac_sample`）

用户裁定块名要自解释：`X`→**`factors`**、`Y`→**`target`**、`sample_input`→**`fac_sample`**
（`amount` 不变），同时删掉 `universe` / `P` / `sample/`（见上文的"删掉的三块"）。
**三个指纹逐位没变**（`panel_digest=798ccb32214853a8`、`amount=79973f9cddb57c50`、
`fac_sample=860cac8991198f88`）—— 因为哈希串里不含块名，见上文。

验收脚本留在 `/tmp/verify_digests.py`（可复跑）。它同时会算一个**对照值**：拿旧块名去查
`files.X` 得到 `d90b20cf37710f94`。两者不同，才说明"改名必须两头同步"这件事是真的。

**两个必须记住的坑**：

1. **脚本自己的字节进上游指纹**。`preparingdata.py::_fingerprint()` 里有
   `h.update(ROOT_SCRIPT.read_bytes())` —— 所以**任何对这个脚本的编辑都会让
   `source.fingerprint` 失配**："无新增"零 I/O 快路径随之失效，下次裸跑会误判成上游变了、
   去重算**上次末日往前 28 个交易日**（当时是 22）。
   ★ 这不危险（重算结果逐字节相同），只是白跑一次；跑完指纹就更新了，**自我修复**。
   2026-09-20 改名时曾用 `--meta-only --freeze` 临时挡住，瘦身时已解冻。
   ★ 要不要把脚本字节从指纹里拿掉是个**设计决定**（见 [TODO.md](TODO.md) 第 10 条），动之前先问用户。
2. **已删的块原先会长回来**：`prepare()` 的两条路径都自动调 `_ensure_prices` / `make_sample`，
   跑一次不带参数的 `preparingdata.py` 就会把 `P`（81 MB）与小样本树（128 MB）**悄悄**恢复。
   已连同构建代码一起删掉。
   ★ 同族的坑：**增量的"丢弃行"告警**。窗口外的行本来就该被丢弃（由 `keep` 提供），
   原先会报"上亿行被丢弃" —— 实测 2026 增量 dropped = 103,349,475 = 145 天 × 2115 只 × 337 因子，
   分毫不差。日跑会**天天误报**，真有脏数据时反而被淹没。已改成扣掉这块再报。

### ★ 下游对齐状态：已收口（2026-09-20 晚）

**`V16/mx/` 已整体删除** —— `V16` 重写为两份自包含脚本：`model.py`（配方 / 数据 / 切分 / 模型 /
损失 / 训练）与 `analysis.py`（推演拼接 / IC / 可执行收益 / 现金回测 / 报告 / 推荐表），
直接读四块 + `prices`，不再有"一套引擎 + 单元配置"的分层，也就没有"下游待对齐"这回事了。
★ `history_iterations/` 里 V12~V14 自带的旧 `mx/` 同样不要再跑。

★ 旧 `mx/data.py::sample_mask` 里 `self.universe & …` 是**恒等化简**（掩码实测恒 True）——
`universe` 块已删，但**"池内 ∩ 标签非空 ∩ 覆盖率达标"这条约束没有取消**：
上游标签本身没做池掩码（池外有值 24,883 格、池内无值 18,308 格）。

#### ── 以下由上游（`featureengineering/`）追加，2026-09-18 深夜 ──

> 用户交办：「类似的下游任务应该**向上游对齐**；你可以好心地把你踩到的坑作为 tips
> 加到下游段的文档中」。下面四条是上游视角的补充，**不替代**上面的实测记录。

**T1/T2（价格轴错位）—— 已无载体，保留机理。**
根因：上游 `fea/prices.py::_place_from` 用「引擎位置」落格、`Panel.place` 按**列号**写进调用方面板，
**默认了**调用方的代码轴 ≡ `Engine.codes`（一个从没写下过的隐式契约）。
根治方向：出口按**代码**对齐（`searchsorted` + gather），或在写之前校验两轴一致并**直接报错**。
★ 现状：`P` 块已删、价格层没有载体，这类事故暂时不会发生；但**任何把价格层加回来的方案
都要把这道校验一起带回来**。另：池子固定 2115 只后 `Engine.codes` 不会再变（冻结的目的），
而**只要冻结名单被编辑，`universe_fp` 就会变** —— 它是 `prepare()` 判"要不要全量重建"的锚点。

**T3. ★ `stock_daily.stock_name` 不是历史名称 —— 别用它判 ST/风险**
上游实测：厂商把**整段历史**的名称都改写成了**当前**名称。实例 `000004.SZ`：
`stock_st_info` 说它 2022-05-06 起才是 `ST国华`，而 `stock_daily` 里**全部 3724 行
都是 `国华退`**（当前处于退市整理期的名字）。
后果：用名称判"曾经 ST"只找到 **284** 只，官方口径是 **784** 只，**漏 502 只**。
判 ST 请用 `stock_st_info`（`type='ST'`）；判板块用代码前缀。
（`model/` 下若只是拿它做展示无所谓，**别进特征/筛选逻辑**。）

**T4. 冻结池的 IC / 收益**不可跨这次变更前后比较**
池子把**退市股与曾 ST 股**永久移出评价域 —— 而它们正是长期最差的一批。
⇒ 同一因子在新池里的 IC 会**系统性偏乐观**，新池净值也不可与旧池净值直接比。
这是"可复现性优先"换来的代价（用户已确认接受），不是 bug。
**做 A/B 时请确保两侧同池**，否则结论无效。

**T5. 上游的变更流程（以后照此协作）**
上游改动因子口径 → ① 上游**负责重建 `trainingdata/`**（现在是"另建再换"：
`--full --out trainingdata_2115` → `--check` → `mv` 换上去，旧目录留作对照组）；
② 上游**主动通知**下游 session；③ 下游不必自建产物、也不必替上游兜 bug，
`DATA` 声明只负责收窄窗口。

★ **2026-09-20 晚更新**：这条与现在的工作方式**已经反过来**了 —— 用户裁定
「每天执行 `python preparingdata.py` 把上游同步到本地」（见本文档 §3「命令」），
所以下游**会**自己跑初加工。上面提到的 `trainingdata-is-frozen-do-not-run-preparingdata`
那条 memory **已不适用**，别再照它拦人。

### ★ 当前快照：唯一一份 = `trainingdata/`（2026-09-22 因子侧去冗余后重新对齐）

用户 2026-09-21 拍板：**对齐到最新因子、只保留一份、且目录就叫 `trainingdata`**
（"model 下只允许有一个叫做 trainingdata 的文件夹，由 `preparingdata.py` 维护"）。

★★ **2026-09-22 重建**：因子侧当天真删了 57 个 |ρ|≥0.95 的冗余因子（656 → 599，见
`featureengineering/HISTORY.md` §2026-09-22「同日追加」）⇒ 本快照按新因子集**全量重建**。
上一版（656 列）**原样保留在 `trainingdata_prev656_20260922/`**（回滚用；确认无误后可删）。

| 项 | 值 |
|:--|:--|
| 目录 | **`model/trainingdata/`**（唯一一份；`trainingdata_v2` 与 337 列旧快照**均已删除**） |
| 维护者 | `preparingdata.py` —— ★ **日常裸跑即可**（默认出口就是 `trainingdata/`） |
| `panel_digest` | **`23a6bfa150528094`**（上一版 656 列是 `71984270753fd665`，已作废） |
| 特征 / 标签 | **599 / 5**（656 − 57 个被删的冗余因子） |
| 轴 | **2018-01-02 ~ 2026-09-21**（2117 个交易日 × 2115 只） |
| 行数 | 4,477,455（factors / target / amount / fac_sample 四块一致） |
| `prices` 指纹 | `3b392f0e09c9589f`（9 年，**与上一版逐位相同** —— 价格块不依赖因子） |
| 列集校验 | 快照 `columns.features` **与因子库 599 个启用因子逐列一致**（2026-09-22 实测） |

★ **重建方式**：`preparingdata.py --full --out trainingdata_new` → `--prices-only`（新快照不会自动补
`prices`，见下方「工具缺口」）→ `--check` 通过 → 换名。**不能只裸跑增量**：因子集变化只改指纹、
不改股票池，增量分支只重写最近 28 个交易日 ⇒ 会得到「老年份 656 列 + 窗口内 599 列」的
**列不一致快照**（与池子变更那条守卫同源，但**没有**守卫挡这一种）。

★★ **仍未做的（模型侧代码）**：20 个模型文件仍写着被删的 6 列
（`chip_resistance_distance`/`chip_support_distance`/`cyqp_average_cost_premium`/
`cyqp_price_cost_position`/`market_cap_concentration_20d`/`ret_skew_60`），其中
`best/features_old337.json`（实战单元）与 `V27~V30/model.py`（硬编码）会**直接报缺列**。
同簇代表见 `featureengineering/artifacts/audits/dedup_20260922/replacement_map.tsv`
（|ρ| 0.95~0.99，可原位替换）。

**改名做了什么**（2026-09-21 晚）：`trainingdata_v2/` → `trainingdata/`，
并同步把 V62~V75 与 `research/` 下 **31 个文件**里的路径常量
`'trainingdata_v2'` → `'trainingdata'`（活跃代码里已无残留）。
验证：`V62/model.py --split` 16 个切分正常、score 窗到 2026-09-21、退出码 0。

★ **一处要记住的副作用**：`RECIPE['snapshot']` 取的就是这个目录名 ⇒ 改名会**换掉 `run_id`**。
但快照内容本来就变了（653→656 列、轴前进一天），**所有版本本来就要重跑对照臂**，
所以这不构成额外损失；只是"复用旧产物"这条路按设计本来就已经断了。

★★ **对齐 = 所有历史结论的锚点变了**。R37 的六臂与 R38 的 k 曲线都跑在
**653 列 / 末日 2026-09-18** 的快照上，而那个快照**已按用户指示删除**。
⇒ 恢复研究的第一步是**锚点复核**：在新快照上跑 V62 的 337 列对照臂
（当日在 653 列上做过同样对拍，结论是 `max|Δ| = 0.00e+00`）。
**注意：旧快照已不存在，这条复核只能验证"新快照内部自洽"，无法再与旧数字逐位对拍** ——
这是用户明确接受的取舍。

#### ⚠️ 两个实测到的工具缺口（下次别踩）

1. **新建快照不会自动带 `prices` 块。** `preparingdata.py --full --out <新目录>` 只建
   factors/target/amount/fac_sample；`prices` 的构建被 `ensure_prices_if_enabled()` 里的
   `if not load_meta(root).get(PK): return []` **挡住了** —— 而新目录的 meta 恰恰没有这个键，
   于是永远建不出来（自举死锁）。**补法**：显式跑一次
   `$PY preparingdata.py --prices-only --out <新目录>`（本次就是这么补的）。
   ⇒ 缺 `prices` 时回测会**硬失败**（`SPEC` 铁律三），不会静默降级，这点是对的。
2. **`--check` 不校验 `prices` 块在不在** —— 本次 `--check` 在缺 `prices` 的新快照上
   **报了"✔ 校验通过"**。所以换快照时**必须自己确认 `prices/` 存在**，
   不能只看 `--check` 的绿勾。

### 快照台账：`log/ledger_YYYY-MM-DD.tsv`（2026-09-20 新增）

一行一个数据文件（四块 × 九年 = 36 行）：`block / year / rows / cols / bytes / sha / mtime / action`，
前几行 `#` 注释带运行上下文。**一天一个文件、同日覆盖**（要的是"最新台账"不是运行流水），
`mtime` 取自文件本身 ⇒ 一眼看出今天动了哪几个。

由 `preparingdata.py::write_ledger()` 写。★ 两个坑（都实测踩过）：

- **挂在 `main()` 不挂在 `prepare()`** —— 台账是**运行记录**，放 `prepare()` 里会让探针/程序化调用也写
  （字节对拍时一天要跑几十次）。
- **键必须是 `(块, 年)`**，不能只按年 —— 否则 `--amount-only` 会把 `amount` 盖到那一年的
  `factors`/`target` 行上，看着像它们也被重写了。
- `action` 由**调用方传本次运行的信息**，不能照抄 `meta["years"][y]["action"]`：
  实测日常增量跑完，那格仍写着上午全量留下的 `'full'`，照抄会误导。

## 4. ★ 切分口径：研究单元 vs 实战单元（★ 2026-09-20 改为**季度滚动**）

| | **研究单元 `V{N}`** | **实战单元 `best`** |
|:--|:--|:--|
| test 集 | **每季度一个窗**：该季度 ∩ 评价窗 **2025-07-01 ~ 2026-06-30**（严格样本外，用来打分/回测/判定） | **没有** |
| train+valid | 该季度**开始之前**、且标签已隔离的历史（`train_end = 季度首日 − (h+1) 个交易日`） | **全部数据**（train_end 放到数据末日） |
| 用途 | 比版本、出结论 | 出当下的选股打分 |

★ **2026-09-20 起是季度滚动**：评价窗内的每个季度**重新训练四折**，历史窗口逐季向前滚动
（旧口径"所有季度训练必须止于 2025-08-10"已废）。因此是 **4 季度 × 4 折 = 16 组模型**。
★ **推演窗（`score`）比 test 长**：从该季度第一个交易日一直到**数据末日**，
支撑增量推演与"最新一日打分"；某一天由「**最新一个没见过它的季度模型组**」给分。
**评价与回测仍只用四季度那一段**（test 是 score 的前缀，代码里有断言守着）。

- 单元在自己的 `model.py` 里声明切分（`V16` 里是 `RECIPE` 的 `test_start` / `test_end` + `splits()`；
  `splits()` 里带四条断言：训练/验证都在季度首日前隔离、train∩valid 为空、test 是 score 的前缀、四窗都非空）。
- **隔断由 purge 自动保证**：标签 `T` 用到 `open(T+1+h)`，所以每个标签的
  **effective train_end = min(train_end, test 起点 − (h+1) 个交易日)**。
  实测（V2 时代，旧数据）：20d 标签退到 **2025-08-01**、5d/1d 停在 **2025-08-08**
  ★ **这组数字已不可复现** —— V2 已归档且无引擎。要复现请用 `V16`：
  `cd V16 && $PY model.py --split`（打印四个季度×四折的 train/valid/test 区间）。
- **`best` 的生产口径由切分配置施加**：研究单元有 test 窗，`best` 没有（并明写
  `valid(★ 实战口径)`、`train_end` 放到数据末日）。⇒ 晋级要保持**纯文件复制 + 只改切分口径**，
  不把研究口径的 test 窗一起搬进实战。★ 旧引擎（`mx/unit.py`）与 `main.py promote` **都已删除**，
  这道保证现在**靠人和 Agent 守**，不再有代码兜底。
- 多折：test 窗**固定不动**（全折共用 ⇒ 指标可比、不泄漏），valid 窗在可训练段内**四等分轮换**
  （`np.array_split(eligible, 4)`，取到的那块当 valid，训练集把该块整段挖掉）。
  ★ 折必须同时换**数据划分**：线性模型给定数据是确定性的，只换种子会得到 4 份一模一样的模型
  （V1 首跑实测 ridge 四折 test RankIC 完全相同 —— 一个假的多折）。

---

### ★ `best/` 目录的约定（2026-09-19 更新，用户要求"尽可能精简"）

> ⚠️ **2026-09-20 状态**：`best/` 目前**是空目录** —— 上一轮那份（复制自 V12）的结论已随数据重建作废，
> 文件已清掉（git 里仍有：`git restore best/` 可取回）。下面的约定仍然有效，等新版本跑出来再按它晋级。

**`best/` 只放脚本与启动文件**：`model.py`（本版配方）· `run.py` · `train.sh` · `analysis.py`
· `version.txt`（一行，记录来源单元）。**不放** README/产物/缓存 —— 说明性内容统一写在本文档。

- 与研究单元**同构**，唯一区别是**切分口径**：`best` 没有 test 窗、`train_end` 放到数据末日。
  ★ 旧引擎会自动丢 test（`mx/unit.py:Unit.split_cfg`），**那份保证已随 `mx/` 一起消失**。
- 晋级入口 `main.py promote V{N}` **已随 `main.py` 删除** —— 现在晋级是**手工复制**：
  把 `V{N}` 的脚本（`model.py` / `analysis.py` / `train.sh`）复制进 `best/`，并改切分口径。
- ★ **纪律：`best/` 里的代码只由 `promote` 写入，不要手工改**；要改就开新版本 `V{N+1}`，
  验证通过后再晋级。
- 日常运维（晋级后）：`cd best && bash train.sh`（用全部数据重训 + 评价 + 出报告，与 V16 同一条流水线）。

### 配方与训练规格（★ **数字不抄，以代码为准**）

配方（网络结构 / 优化器 / 损失 / 标签 / 种子 / 特征集）的权威定义在 **`V16/model.py` 的 `RECIPE`** ——
本文档**故意不复制这些数字**（复制就会和代码不同步）。这里只记**改了会怎样**：

- **`run_id` = 配方 + 各数据块的清单 SHA**（`factors` / `target` / `amount` / `fac_sample` / `prices`…），
  **不含源码字节** ⇒ 整理代码不会误伤产物；但**配方或数据一变，旧产物就不认了**。
- ★ **不能手改锁文件骗过恢复检查**：改了配方或重建了数据，正确做法是**先归档旧产物**再重跑
  （`history_iterations/` 是归档位）。
- ⚠️ **有一个例外：验证指标 `val_wei` 的系数是硬编码、不在指纹里** ⇒ 改它**不触发重训**，
  但它决定"选哪个 epoch / 哪份权重" ⇒ 必须**同时归档产物或改 `valid_metric`**，否则会拿旧权重
  配上新的选择标准、产出来源不明的结果。该探索见 [TODO.md](TODO.md) **RD3**。
- **每 epoch** 存 `last.pt`（含优化器 / 学习率 / 随机数状态，可续训），**`best.pt` 只存最优权重**；
  已完成折校验指纹后跳过。
- **测试窗只用来打分**，不参与 epoch 与超参选择（早停看验证块）。

---

> ★ **§5 / §6 / §6.5 已作废**：那几节讲的是旧 `mx/` 引擎副本（`mx/labels.py` / `mx/losses.py`），该副本已随 2026-09-20 重构整体删除。现役损失函数与训练纪律写在 **`V16/model.py`** 里。编号不回收。

---

## 7. 评估三层与策略口径

### ★ 口径速览（本轮评价与回测的确切参数）

先拼接各季度**严格样本外**预测，再评价完整信号窗 **2025-07-01 ~ 2026-06-30**（242 个交易日）。
**四折共用同一日期窗** —— 折间的区间体现的是切分/种子差异，**不能当四个独立年份**读。

现金策略**固定比较四条**：top1 每日 · top5 每日 · top5 每 5 交易日 · top5 每 20 交易日。
**单次买入不超过信号日成交额的 1%**；资金不足保持现金；最后信号后的**第二个开盘尝试清仓**，
卖不掉的持仓保留估值并**明确记录**。同时报告**最大回撤**、费用、交易数、末尾未平仓数、
**去掉最佳 1/2/5 日**的收益与 **H1/H2 分半**。

**公司行为近似**：用**复权比折算经济股数** ≡ 分红立即再投资，**不是**逐笔分红现金到账复原
⇒ 这套东西叫「**含费用现金账户模拟**」，不声称与券商交割单完全一致。

没有 `prices` 就**硬失败**；`--no-backtest` 明确只出 IC，**没有静默收益降级**。


### ★ 术语约定（2026-09-19 用户拍板，全库统一）

| 写法 | 含义 | 引擎字段 |
|:--|:--|:--|
| **RankIC** | **逐日截面 Spearman 秩相关**的日均值 —— 本库说的"IC"**一律指它** | `rankic_mean` |
| Pearson IC | 逐日截面 Pearson 相关的日均值。**提到它必须写全"Pearson IC"**，不许简写成 IC | `ic_mean` |
| **ICIR** | **RankIC 均值 ÷ RankIC 标准差**（信息比率），是"稳不稳"的度量 | `rankic_icir` |

★ 为什么要把这条写下来：本库的**主判据是 top_ret 而不是 IC**（[LESSONS.md](LESSONS.md) §8.11 实测两者会反向），
所以"IC"这个词一旦含义漂移，读表的人就会把两种不同的相关性混为一谈。
旧文档里凡是只写"IC"的地方，**一律按 RankIC 理解**；表头里的「RankIC」= RankIC。

| 层 | 内容 | 口径 |
|:--|:--|:--|
| **IC 层** | 榜单：RankIC / ICIR / t / 分层 D9−D0 / top-N 换手 | **逐日截面** IC 序列 → 日等权平均；ICIR/t 自己算（模块② 的 `summary.json` 里 ICIR 全是 NaN） |
| **主判据层** ★ | `evaluate.costfree_cumsum`：**买 top-n 隔日换手的收益累加**（Σtop1 / Σtop5 / 超额 / 等权全买对照） | **必须套"次日开盘买得进"掩码**；累加不连乘（用户 2026-09-18 拍板） |
| **策略层** | `V16/analysis.py`：**现金级真实净值** + 策略矩阵（策略 × 打分源 × 折） | 起始 10 万 · **≤5 只** · T+1 开盘 · 整手 · 含全部费用 |
| **稳定性层** | 折间离散度（榜单）+ 折间区间（策略矩阵） | H1/H2 分半、bootstrap 夏普 CI **已就位**；★★ **剔 1 天 / 剔 2 天**也已进榜 —— 这是**必看项**：实测 top1 往往只集中在 2~3 只票上，去掉最好的 5 天就转负（见 [LESSONS.md](LESSONS.md) §8.1 R4） |

### ★ 主评估指标：四个（用户 2026-09-20 拍板）

用户 2026-09-20：以参考工程的模型评测 notebook（`tmp/min_test.ipynb`）为参照，
**主评估指标定为下面 4 个**；**多样性熵 DE 不算单模型指标，不纳入**。

| # | 指标 | 本库口径 | 现有产物 |
|--:|:--|:--|:--|
| 1 | **IC** | 按本库术语约定（上一节）= **RankIC**。★ 参考库那个 `IC` 是 **Pearson**（`score.corr(ret)`），**两者不可横比** | `report.json` → `variants[*].IC` |
| 2 | **ICIR** | RankIC 均值 ÷ RankIC 标准差 | 同上 |
| 3 | **top 收益** | 本库对应物 = **ΣtopN**（可执行、无摩擦、累加）。★ 参考库是"按流动性买满 **15 亿**、最多 **500 只**"的日度均值，**量纲与口径都不同** | `variants[*].costfree` |
| 4 | **top 收益稳定性** | 逐日 top1 收益序列的**均值 ÷ 标准差**（参考库口径，**不年化**；×√242 即年化） | ✅ `main_metrics_1d.csv` |

**★ 已定的口径（用户 2026-09-20）**：

1. **期限：全部取 1d** —— IC 用 **1d RankIC**、top 收益用 **Σtop1**（1d 标签累加）。
   ⇒ 报告不再只展示 5d RankIC：**1d 是主指标的口径**，5d 退为参考（它与训练标签匹配，不是交易口径）。
2. **top 收益 = Σtop1**（可执行、无摩擦、累加）。Σtop5 与其余策略保留在策略层作参照，不进这张主表。
3. **稳定性按"行内逐日"算**（见下）。

**★ 输出形式（用户 2026-09-20）：分季度输出，4 个季度 + 一行总计 = 5 行。**

| | IC | ICIR | top 收益 | top 收益稳定性 |
|:--|--:|--:|--:|--:|
| 2025Q3 | | | | |
| 2025Q4 | | | | |
| 2026Q1 | | | | |
| 2026Q2 | | | | |
| **总计（全年 242 个交易日）** | | | | |

- 每行 = **该季度内的** 4 个指标（IC/ICIR 用该季度的逐日 IC 序列；top 收益与其稳定性用该季度的逐日 top 收益序列）。
- 总计行 = **全年**（不分季度）的同一套算法；不是"把 4 个季度的数再加权平均"。
- ✅ **已落地**：`model_pred/tables/main_metrics_1d.csv`（由 `analysis.py` 计算、`generate_report` 渲染进
  `REPORT.md` §1 的「★ 主评估指标（1d 口径）」小节）。算它需要的逐日序列来自 `costfree()` 的 `return_value`。

---

### ★★ 2026-09-22 变更：主指标口径 **1d → 5d**（用户拍板）

> 用户 2026-09-22：「相关的指标也从 1 日的 IC 改成 5 日的」。
> **研究范围同时收紧为两个方向** —— 详见 [`TODO.md`](TODO.md) 顶部的
> `<!-- USER_TWO_DIRECTIONS_20260922 -->` 块（**RD11** 五日换手＋止盈止损 · **RD12** 打板策略）。

| 项 | 旧（2026-09-20） | **新（2026-09-22 起）** |
|:--|:--|:--|
| **IC** | 1d RankIC | **5d RankIC** |
| **top 收益** | Σtop1（1d 标签累加） | **Σtop1（5d）** |
| **ICIR / top 稳定性** | 由 1d 逐日序列算 | **由 5d 序列算** |
| 交付表 | `tables/main_metrics_1d.csv` | 新增 **`tables/main_metrics_5d.csv`**；**1d 表保留不删** |

- 分季度输出形式不变：**4 个季度 + 一行总计 = 5 行**。
- 技术上这是**口径变更，不是新指标**：`costfree()` 已有 `horizon` 入参，
  改的是取哪条标签的逐日序列，`analysis.py` 需同时出两份表。
- ★ **1d 表不删** —— 全部历史 `REPORT.md` 与本文件上面的内容都锚在 1d 上，
  删了就**没有对照组**。两份表并存，**引用时必须写明是哪一份**。

★★ **两条必须跟着写的代价**（否则读数会被误读）：

1. **不是"加一列"，是换榜**。`LESSONS.md` **R34** 实测 IC 与 top 收益在标签期限上是
   **同一旋钮的两端**（权重每 +0.25，IC 涨 ~+0.0006、Σtop1 掉 ~0.25）；**R32** 实测 3d 是峰
   （3d > 5d > 1d，10d/20d 明显变差）。⇒ 换成 5d 会**同时改变"谁排第一"**。
2. **`LEADERBOARD.md` 现存行全是 1d 口径**，与新行的 5d 数字**不可横比**。
   按本库纪律（旧结论原样保留、只加标注）**旧行不重算**，只标注口径。

---

### ★★ 执行口径第二套：**打板**（RD12，2026-09-22 新增，与上面这条**并行不覆盖**）

> ⚠️ 这一节与下面「★ 主判据」里的**开盘限价拒单**是**两套互斥的口径**，
> 不是修订关系。原文如下（仍在服务 RD11 与全部历史版本）：
>
> > **开盘价拒单口径**：开盘价触及**按 `pre_close` 算的 10% 主板限价**即保守拒单，停牌拒单；
> > **不用 T+1 全天 high/low 去决定"开盘买不买得进"**。
>
> 这条规则**系统性拒绝涨停价位的一切成交**，而**打板买的正是这个价位** ⇒
> **打板不能复用这套入场 / 拒单路径**，必须另写执行口径；**两套收益不可横比**。
>
> 完整的打板口径（涨停价定义 · 触板/封板/一字板判定 · 入场出场 · **信号时点错位** ·
> 三档成交率敏感性 · 五条偏差声明）写在 [`TODO.md`](TODO.md) 的 **RD12** 小节，
> **本节不复述数字**（复述就会和那边不同步）。
>
> ★ 最容易写错的一条：打板要在 **T 日盘中**下单，而现有 `score` 是「**T 日收盘后**」算的
> ⇒ 必须用 **T−1 收盘出分 → T 日盘中执行**的错位口径。直接套现有 `score` 等于用
> 「T 日收盘（含 T 日涨停这个事实本身）」预测「T 日盘中的涨停买入」——
> 是 `R3`（掩码必须对齐**成交日**而非信号日）的翻版，**写错了不报错、只让收益虚高**。

### ★ 主判据（可执行的无摩擦口径）

用户 2026-09-18 定：**"baseline 是买 top1 隔日换手收益，收益用累计求和即可，没必要乘除法"**。
实现是 `V16/analysis.py::costfree`，每天：

1. 收盘后拿到打分 → 次日开盘买入分数最高的 n 只 → 再下一日开盘卖出并换仓；
2. 一天的收益 = 所持 n 只的 `label_ret_1d` 等权平均（`= open(T+2)/open(T+1) − 1`，正是这一个动作）；
3. **累加**求和，不连乘。

★★ **`enterable`（次日开盘买得进）不是可选项**。踩过的坑（[LESSONS.md](LESSONS.md) §9 有实测数字）：
不套这个掩码时 `gbdt_w/5d` 的 Σtop1 = +3.638，套上之后只有 **+0.289** ——
因为 **37.9% 的交易日**它选的是次日一字涨停/停牌的票，那些天平均 +3.49%、买得进的天只有 +0.18%。
**92% 的"收益"根本买不到。** 模型会自发往这个方向收敛（涨停在动量/量比/涨停计数里可见），
所以任何不看可执行性的目标函数都会优化一个**做不到**的东西。
这不是交易摩擦（费用/滑点），是**可执行性**，属于模块③ 的硬约束 #4。

`V16/analysis.py` 的默认路径一律套真实可买掩码。

> ★ **价格层现状（2026-09-20 晚：已重建）**：可买掩码走 **`trainingdata/prices`** 块
> （`trade_date / stock_code` + `open / high / low / close / pre_close / pct_chg / vol / adj_factor`，
> 按年分区，float64 原值、缺失留 NaN）。它是**回测原料**，与四块训练数据分开维护，
> 唯一写入口是 `$PY preparingdata.py --prices-only`；写入时**按 `(trade_date, stock_code)` 键
> `reindex` 落到面板格子上**（重复主键直接报错），不按列号。
>
> ★ **拿不到就硬失败，不许静默降级**（用户拍板）。原来的写法是拿不到价格层时打一行 ⚠️
> 就继续跑、返回 `exec_filter=False` —— 那正是"回测路径自诞生起就没跑通过、谁也没发现"
> 那次事故的成因（见 [LESSONS.md](LESSONS.md) §11）。**`except` 兜底必须同时兜住"功能没了"这件事。**
> 只看 IC 层用 `--no-backtest`（IC 不碰价格层）。
>
> ★ **开盘价拒单口径**：开盘价触及**按 `pre_close` 算的 10% 主板限价**即保守拒单，停牌拒单；
> **不用 T+1 全天 high/low 去决定"开盘买不买得进"**。这比历史上的一字板过滤更保守，
> 所以**不能与旧收益数值直接横比**。

**策略约束**（用户 2026-09-17 裁定，小资金）：起始 10 万 · 最多 5 只等权 · 每日只有
hold/clear/switch 三个动作 · 第二天早盘执行（一字板/停牌顺延）· 费用 = 佣金万 2.5（最低 5 元）
+ 印花万 5（卖）+ 过户万 0.1 + 滑点 3bp。**不许用"逐笔满额复利"**（参考工程实测多仓会被系统性
高估：+377% vs 真实 +126%），本引擎只出真实净值。

**回测只跑评价窗**：研究单元 = test 窗（样本外），实战单元 = 最后一段验证窗（并明写
`valid(★ 实战口径)`）。★ 跑全样本的净值含训练期，是自欺（第一版实测：全期 +42.6% vs test 窗 −1.6%）。

**榜单是在评价窗上排的**，用它选版本存在选择偏差 —— 所以结论必须写清"这是 test 选出来的"，
并用折间离散度兜底。**单种子纪录 = 种子中奖**（参考工程 V29 同配方换种子 +73.7% → +2.7%），
任何"新纪录"必须多种子复刻。
