<!-- USER_WIND_DOWN_20260921 -->

<!-- REGISTERED_RESEARCH_FINAL_20260921 -->
## 当前状态：预定研究已全部完成，按用户要求暂停

V41–V43和V56–V61及原定配对、资金组合、留一、成本和流水核验均已完成。没有新增方向或版本，没有恢复V44–V49。后面的历史记录保留研究过程，其中旧“待执行”状态不再代表当前任务。完整结论见V56/short_training_research/WRAPUP.md，三轮明细见同目录FINDINGS.md，最终证据审计见V56/logs/research_wind_down_audit.json。

## 用户最新指令：完成现有设计后暂停

2026-09-21：停止提出或启动新研究方向、新版本、新因子。仅完成当前V43与已登记V56–V61，以及其原定完整回测、配对、实际资金组合与流水审计；记录所有正负结果和经验后暂停。V44–V49继续取消。该指令覆盖本文历史上任何继续扩展研究的待办；不因结果不佳追加参数、种子或策略。运行中仅修复完成既定实验所需的执行/审计问题。90GB内存上限、84GB提前停止保持。收尾状态和检查清单见`V56/logs/research_wind_down.json`；汇总见`V56/short_training_research/WRAPUP.md`。

## 2026-09-21 用户范围收紧：仅限 model（覆盖之前队列安排）

用户明确禁止本Agent考虑或开发新因子；新因子由其他Agent负责。本Agent只在 /root/autodl-fs/model 内继续工作：冻结337列已有因子的模型侧筛选、模型训练优化、交易策略验证。禁止增加新输入列，禁止继续上中游方向。

V44–V49未启动的新增输入实验已取消，每个版本有SCOPE_CANCELLED.json；两个协调脚本已加拒绝重启检查。等待中的一致性协调进程已改为直接等待V53–V55，顺序为 V53→V54→V55→V41→V42→V43。V54正在执行，未中断已授权模型训练。取消与新进程记录见V53/logs/model_only_scope.json。历史记录保留，不代表后续计划。

补充模型侧成本分析：全部27种已有策略，固定单边3/10bp，完整报告三组筛选配对；在V53结果之后、V54/V55结果之前登记，属于次要探索，主比较不变。3bp应复现主报告，每个账户独立复算现金与持仓。仍遵守84GB提前停止、90GB硬上限，重训练和完整分析串行。

# 模块③ · 经验与结论（LESSONS）

> **结论都在这里**：哪些规律已经定论（`R*`）、哪些只在一份文档里留过实测（`U*`）、
> 哪些方向**验证过不要重试**、哪些坑踩过。由旧 `LESSONS.md`（去掉 V1~V15 逐版履历）+ `LESSONS.md` 合并（2026-09-20）。
>
> ★ 每条都注明**当初测在哪份数据上**与可信度（定论 / 未观察到效应 / 候选未复现）。
> 引用任何旧数字前，先核对它建在哪个 `panel_digest` 上：数据换过两次
> （股票池 3485 → 2115、因子集 306 → 337 特征），**跨这两次变更一律不可比**（见 [`SPEC.md`](SPEC.md) §3）。

> **本文件包含**：§8 核心结论与规律（§8.1~§8.5 分组 / §8.6 只此一份的实测 / §8.7 参考工程复刻遗留）
> · §9 已确认的事实 · §10 死胡同清单 · §11 坑清单 ｜ **文档入口**：[`README.md`](README.md)
> ★ 编号空间：本文 `R*` = 核心结论 · 本文 §8.6 `U*` = 单文档实测 · [`TODO.md`](TODO.md) `RD*` = 研究/待验证方向。
> 旧 `LOG.md` 的 `D1`~`D5`（缺陷记录）随文档收敛一并归档，见 `history_iterations/docs_archive_20260920/LOG.md`。
> 章节号（`§N`）**全库统一**，跨文件引用写成「文件名.md §N」并带链接。

---

## 8. 核心结论与规律

### 当前连续迭代记录（2026-09-21 14:39；分阶段证据，不冒充最终定论）

用户当前明确优先提高**稳定的含费用净收益**，不能依赖幸运种子；IC仍报告，但不能用IC替代账户收益。固定评价窗与快照不变，所有新研究仍受反复研究同一时间窗和固定股票池幸存者偏差影响。

- **环境恢复是吞吐改善，不是算法收益改善。** 原torch2.14+cu130无法使用现驱动GPU，已在同一共享Python切换官方torch2.11+cu128；CPU/GPU前向、梯度和优化器实测通过，旧依赖已归档供回滚。V32的16模型与完整审计约18分钟，GPU计算阶段96%–99%，内存峰值68.78GB，无OOM。跨CPU/GPU比较收益仍不能作训练改动的因果证据。
- **小排名缓冲没有显示稳定增益。** 固定V22/V28/V29/V30的4折与集成，共140账户；Top1退出排名2/5/10的集成平均收益差为−1.39/−20.33/−7.15个百分点；Top5退出排名10只在2/4集成上提高净收益。没有依据继续围绕峰值加密搜索。见[V32/strategy_research/REPORT.md](../V32/strategy_research/REPORT.md)。
- **固定60日市场门槛是候选，尚不是跨月份稳定结论。** 在观察2026Q2共同亏损后提出，未扫描均线长度；仅用当日及此前复权收盘等权指数，T收盘判断、T+1开盘执行。V22/V28/V29/V30的Top1/Top5共8个集成都改善收益与回撤，V19/V26/V31的6个集成方向也复现，但其中3d全特征Top1仍亏损。不能把“比原来好”写成“已经赚钱”。新GPU V32/V33也复现门槛改善方向，后续种子仍在训练。
- **门槛改善仍有时间段依赖。** 旧四版本同日配对、20交易日区块重采样2000次，保留版本间市场相关性；Top5群组对数财富比均值95%区间[−0.242,+0.476]跨零。此诊断未校正重复查看研究窗和提出策略的选择偏差，不是未来盈利概率。见[V32/block_robustness_legacy/REPORT.md](../V32/block_robustness_legacy/REPORT.md)。
- **交易成本与收益集中度不能省。** 固定3/6/10bp滑点，3bp逐项复现原账户。V32 Top5＋门槛在10bp时+26.00%，但后半程−6.94%、剔最好5日仅+0.07%；V33对应+9.07%、−5.22%、−11.45%。正的全年数字不能独立支撑“稳定收益”。V32/V33门槛账户另以保存的逐笔成交独立重建现金和持仓，每日净值差小于1e-6元，费用、T+1、整手和1%前日成交额上限通过，见各market_gate_new子目录ledger_replay.json。
- **近期样本加权三组均不支持Top1改进，不采用。** 固定种子3253/3254/3255，0对504交易日半衰期：三对净收益差−109.95、−39.64、−71.56个百分点；平均−73.72个百分点。Top5也不能仅凭首组小幅提升下结论。全部结果与同环境、同数据、同种子锁文件核验见[V32/paired_research/REPORT.md](../V32/paired_research/REPORT.md)。
- **宽松折间共识过滤是无效操作，不是新alpha。** 固定“至少2/4折排入前50”，仍按原始四折分数直接相加；旧四版本×Top1/Top5×有无市场门槛的16账户，逐笔交易与原账户完全一致。该条件没有改变实际交易，不再围绕它扫描门槛。见[V32/consensus_research/REPORT.md](../V32/consensus_research/REPORT.md)。

- **固定三种子实际资金组合**已完成全部28账户：V32/V34/V36各1/3初始资金，共10万元；重新计算整手、费用与共享1%容量，不平均三个10万元账户。加固定MA60门槛后3bp净收益+130.67%、最大回撤−10.72%、后半程+19.59%、剔最好5日+45.53%；10bp下+92.59%、−11.20%、+12.00%、+22.04%。逐一移除任一模型的三种10bp组合净收益+82.34%至+113.42%，后半程与剔最好5日均正。独立逐笔重建全部28账户通过，最大净值误差8.74e-10元。仍不能称时间稳健：Q2为−3.85%/−5.41%，20日区块相对财富改善95%区间跨零。组合想法提出于观察V32/V34后，仍属同窗探索；见V32/portfolio_research/{REPORT,STABILITY}.md及independent_ledger_audit.json。

- LayerNorm三组V38–V40及全部资金组合完成：Top5每日换手仅1/3改善；Top5＋门槛10bp为+31.25%/+15.21%/+12.28%，三组后半程均负。原固定三模型Top1资金政策用于V38/V39/V40后，10bp净收益+21.87%、回撤18.72%、H2−6.21%、剔最好5日−19.71%，弱于原BN组；不采用为稳定收益升级。全部28账户独立流水复算通过，见V38/normalization_research/及V32/portfolio_families/normalization/。
- 固定100次随机选股负对照已完成：同10万元、三个子账户、同有效股票集合和费用/10bp/市场门槛，随机收益中位数−32.27%（有门槛）/−45.57%（无门槛），原模型组合+92.59%/+42.56%；模型超过100/100模拟，不能解释为显著性或未来盈利概率。随机分数持仓持续性不同，费用换手差异也需保留。首个固定模拟两账户独立流水通过，见V32/random_selection_control/。
- 原始分数尺度诊断：V32/V34/V36/V38的典型单折标准差占总和25.8%–32.2%，未见明显单折垄断；不据此增加推理归一化或优先改尺度。诊断不改任何分数，见V32/score_scale_diagnostic/。
- V47–V49稀有价格事件输入已登记并通过CPU/GPU验证：冻结快照中2025连续跌停rank有233/243天恒定，其中190天仍有四舍五入10%下价位事件。追加当日上下价位代理及各自5日频率4列，输入341；原因子/资格不变，只读冻结prices。数值代理不等于原±9.8%因子或完整交易所限价认定。已验证取整、无量、缺失、暖启动及未来截断；排在市场状态输入之后。
- 优先级调整：原三组基础种子3253/3254/3255在折内seed+fold−1规则下有初始化重叠。V50/V51/V52固定新种子15361/28403/49117，每个四折范围互不重叠且不重叠旧3253–3258；原配方完全不变，仅作初始化复核，不挑选成绩最高的新旧组。所有三模型/移除组合都报告。当前执行顺序V50–V52 → V41–V43 → V44–V46 → V47–V49；已只重启尚未训练的等待协调器，旧状态已存control_before_seed_reprioritization.json。
- 所有已登记方法均采用同一三模型资金政策和相同3/10bp组合、全部逐一移除检查；计划见V32/portfolio_families各目录。含末日拒卖未清仓时，股票贡献使用独立流水的期末估值，不排除账户、不虚构成交。

以上为当前连续研究记录；下方历史条目的数据、设备及时期按原文保留。

### 8.1 主判据与口径

**R1. 决定盈亏的第一变量是「标签期限」，不是损失函数、也不是掩码。** —— **定论**
- 同一批策略、同一个单元，只把打分源的标签从 5d 换成 20d，现金级收益从 **−40% ~ −97%** 变成 **+5% ~ +50%**。
- 机制：成本约 **0.15%/次往返**，持有 1 天的策略一年要付 **~35%** 的成本；持有 10~20 天降到 1/10 ~ 1/20。
- 出处：V3/V4/V5，旧池 3484 只、test 窗 2025-09~2026-09。★ **已在新数据上重现**：
  V16 首跑（2026-09-20，337 特征 × 2115 只）20d 是唯一可用的一条腿、1d 全负。

**R2. 不看「买不买得进」的 top_ret 是假的；模型会自发学会挑「明天一字板」。** —— **定论**
- V3 折 1 `gbdt_w/5d`：**37.9% 的交易日** top1 次日一字涨停/停牌买不进；那些天平均 **+3.49%**，
  买得进的天只有 **+0.18%**。不套掩码 Σtop1 = **+3.777**，套上正确掩码 = **−1.576**（复利 −87%）。
- 模型 top1 落在"买不到"的格子上是基准的 **42×**，落在「T 日收盘涨停」是 **17×**。
- ⇒ 已固化为铁律（[SPEC.md](SPEC.md) §2 铁律三、[SPEC.md](SPEC.md) §7）。

**R3. 掩码必须对齐到【成交日 T+1】，不是信号日 T。** —— **定论**
- V3 折 1 `gbdt_w/label_ret_5d` 253 天：不套掩码 **+3.777** / T 日掩码（**错位**）**+0.637** /
  T+1 日掩码（正确）**−1.576**（复利 −87.0%）。修完主判据与现金级策略（−93%）终于对上。
- **这类错位不报错、不崩，只会让一个亏损的策略看起来在赚钱。**
- ⇒ 涉及"信号日/成交日"的数组，调用点必须显式写明对齐哪一天。详见 [SPEC.md](SPEC.md) §2 铁律三。

**R4. 读任何 Σtop1 必须先看「剔1天 / 剔2天 / 集中度」。**
- V3 折 1 `gbdt_w/20d` 的 top1 在 253 天里只集中在 3 只票上（603378.SH 44 次 / 600117.SH 23 次 /
  600691.SH 20 次），且**去掉最好的 5 天后 Σ 转负**（+0.248 → −0.327）。
- ⇒ 任何"新纪录"先过这一关：一天就撑起整个 Σ 的，换个种子大概率就没了。

**R5. 两套独立口径（无摩擦累加 vs 现金级含费用）必须互相印证；对不上时先怀疑口径。**
- 一个说赚钱、一个说亏光 ⇒ **必有一个错了**。修好 R3 之后两边终于对上。

**R6. 「RankIC 最高的头」与「Σtop1 最好的头」是两个不同的头；头的强弱排序会跨窗口翻转。** —— **定论**
- V12：`xgb`/20d IC **全场最高 +0.1352**，Σtop1 **−0.177**；`nn_cons`/20d IC 更低（+0.1056），
  Σtop1 **+0.566**。换池对照：IC 几乎不变（+0.1262 → +0.1352），top1 完全翻转（**+1.143 → −0.177**）。
- ⇒ **判定用 Σtop1，RankIC 只作参考**，两张榜要摆在眼前。

### 8.2 模型与损失

**R7. 「直接对着截面最头部优化」= 对着噪声拟合 —— 跨 4 个版本、两种模型族、三种实现，全部为负。** —— **定论**
- `xgb_rank` 5 个标签 valid/test **全负 IC**；`xgb_rank100/50` 修了取样对数后仍是 −0.008 ~ −0.060
  （**剂量响应不存在**）；`nn_top`（`L_top = −Σ softmax(s/τ)·ret`）是三个网络头里最差的；
  `nn_aggr`（`L_top` + ListNet + 尖锐 τ）20d **−0.0006**。
- 共同解释：**截面最头部那几只的信噪比太低**；跨整个截面平均的目标（MSE/IC）反而稳。
- ⚠️ `nn_aggr` 那次把"激进"与"小网络 + 低 dropout"混在了一起，**不能单归因**。
- ⇒ 已进 [LESSONS.md](LESSONS.md) §10 死胡同。

**R8. 排序目标（lambdarank/NDCG）在这套任务上稳定不行。** —— **定论**
- V4 首跑 `best_iteration` 只有 **24**（总 400 轮）⇒ 排序接近随机且方向是反的。
- **方法论**：**自定义目标的早停指标本身要当超参对待**；且**子集上的参数挑选不能当结论** ——
  子集的年份、股票池、市场状态都不代表全样本，参数选择必须在正式切分上做。

**R9. 两折的差值不构成结论 —— 本库折间跨度就有 ±0.03 ~ 0.09。** —— **定论**
- V5 折 1/2 时掩码看起来"明确有效"（10d +0.032→+0.070、20d +0.071→+0.114），
  **四折跑齐后全部 |Δ| ≤ 0.016、多数 ≤ 0.005**；换 XGBoost 实现（V8）直接反号。
- ⇒ 任何"某改动有效"的草稿结论，先看它有没有超过折间跨度，没有就写"未观察到效应"。

**R10. 可执行性掩码（把"买不进"的行从训练样本剔掉）是死胡同：无效，且在长期限上有害。** —— **定论**
- V5（LightGBM）Δ 全部 ≤0.016（落在折间跨度内 ⇒ **未观察到效应**）；
- V8（XGBoost）**同折同种子**干净对照，方向一致为负：1d +0.021 / 3d −0.006 / 5d −0.010 /
  10d −0.014 / 20d **−0.018**。
- 机制推测：训练时看不见 ⇒ 打分时是"没学过的外推值"，而测试集是全截面，它们照样被打分、照样可能排前面。
- **★ 活路**：若要在训练侧剔，就必须**在打分侧也剔**（把 T 日涨停/停牌的票直接置 NaN）——
  现在只剔了训练侧。详见 [LESSONS.md](LESSONS.md) §10 详录。

**R11. 神经网络这条线是通的；「保守」配方压倒「激进」配方。** —— 配方级推荐（数字随数据变，方向保留）
- `nn_ic`（MSE + 当日截面 IC 项）5d test RankIC **+0.0662**，折间很稳，横向比同期限所有头都高；
- `nn_cons`（MSE 为主 + 中等 IC + R-Drop、512-128、dropout 0.4）5d **+0.0693** / 20d **+0.1190**；
- `nn_aggr` 20d **−0.0006**、折间 −0.044~+0.081（很散）。
- ⇒ `TODO.md` 已把 `nn_cons` 定为"上一轮在线的最好配方"，新数据上要重新验证。

**R12. XGBoost-CUDA 可以替代 LightGBM：IC 等价、快 5 倍。** —— 技术事实，与数据无关
- `xgb`/20d test RankIC **+0.1216**（`gbdt_w` +0.1208，几乎等价），折间更窄，
  单标签 **~33 s vs ~150 s**。**LightGBM 官方 wheel 是纯 CPU**，树的 GPU 路径只能走 XGBoost。

### 8.3 集成与合并

**R13. 集成能提 IC 但会拉低 Σtop1；且「集成口径（`ens:`）普遍好于逐折口径（`f{k}:`）」。** —— **定论**
- `xgb` 策略层 +129.4%（夏普 +0.73→+2.14）**单折复现不出来**（4 折里只有 2 折变好）；
- 机制：集成打分是 4 个种子的平均，好成绩里有相当一部分是"**噪声被平均掉**" ⇒
  **看规则好不好要以逐折为准**。
- ★ **已跨数据复现**：新数据 V16 上集成 +0.1379 IC / Σtop1 **+0.357**，而单头 `xgb`
  +0.1233 IC / Σtop1 **+0.857**。

**R14. 「两个低相关模型集成会更好」：现象存在，但对"钱"没用。** —— **定论（在 ≤5 只 / top-1 主导的口径下）**
- 相关性矩阵确实低（最低 **+0.431**）⇒ 用户描述的现象存在；
- 但 `decorr_w` 把 RankIC 抬到全场最高 **+0.1288**，Σtop1 却 **−0.018**；而单头 `best1` Σtop1 **+0.393**。
- 机制同 R6/R15：**Σtop1 由第 1 名决定，而任何加权混合都会稀释 top-1**（混合出的第 1 名是折中票）。
  ⇒ 真正的杠杆是**别让 top-1 被稀释**。
- ⚠️ 附带工具缺陷：`greedy_lowcorr` 的阈值 0.90 比池内最大相关 0.804 还宽 ⇒ 6 个头全选、
  与 `equal` **逐位相同**。阈值需按池内相关分布自适应。

**R15. 等权是最差的合并规则；`rank_eq`（先秩化再等权）是最好的折中 —— 但这条优势在独立模型上没复现。** —— **候选级，未复现**
- 六规则 test 窗 Σtop1：`equal` +0.020 / `pos_ic` +0.020 / `icir_w` −0.140 / `best1` +0.566 /
  `best2` +0.321 / **`rank_eq` +0.470**（IC 只比最强的低 0.008，Σtop1 是等权的 23 倍）。
- **反证**：在 6 头跨单元池上 `rank_eq` Σtop1 = **−0.205** —— **优势没有复现**。
- ⇒ 作者自己写明"本节的排序是用 test 窗排出来的 ⇒ 只能当候选；**在复现之前不许用它晋级 `best`**"。
- ⚠️ `pos_ic` 与 `equal` 在该单元上**逐位相同**（三个头的 valid IC 全为正 ⇒ "只取正 IC" = 全取）
  ⇒ **报告里出现两行完全相同的数，先查是不是「等价退化」，别当成两条规则互相印证。**

**R16. Σtop1 由第 1 名决定；`nn_cons` 的 top-1 是真信号，`xgb` 的 top-1 是系统性错误，等权混合出的 top-1 是"死位"。**
- 名次剖面（test 窗已套 T+1 掩码）：`nn_cons` 5d 第 1 名 **+0.370%/天**、20d +0.241%（唯一稳定显著为正）；
  `xgb` 第 1 名在**所有标签上全为负**（−0.075 ~ −0.916）；V12 单元 20d 第 1 名 **+0.009%**，
  而它的 **2~5 名均值三个标签全正**。
- ⇒ 「等权把三个头混成一张折中票，谁都不认 —— **根因在合并，不在模型**」。

### 8.4 方差与晋级纪律

**R17. 模型层极稳、组合层极抖 —— 方差几乎全部来自"只持 5 只"这一层。** —— **定论**
- 四个种子集（`make_splits` 的切分**只由折号决定** ⇒ 四个种子集的 train/valid/test 逐字相同，
  测的确实是"换种子"不是"换窗口"）：策略收益 +17.53% ~ +37.92%，**极差 20.4 个百分点**；
  而 `gbdt_w`/20d 的 RankIC 依次 +0.1191/+0.1201/+0.1183/+0.1164，**极差 0.0037（相对 3%）**。
- ⇒ 作者自标"**方向依然成立**"：晋级条件改成"先把策略层的方差压下来，再晋级"。

**R18. 「单种子纪录 = 种子中奖」—— 必须先做种子冗余再谈晋级。**
- V5 的头条数字 "+49.25%" 取自策略矩阵里恰好是四个种子集里**最好**的那一格；同一格真值是
  **+28.96% ± 10pp**。
- 附带方法论：读策略矩阵必须看"打分源"列是不是**单元组合** —— `ens:<单元名>` 与 `ens:<单头>`
  是两个不同的东西，别混着引用。

**R19. 晋级看「四折全正条数」，不是最高均值。**
- V12 = **4 条**四折全正的 ≤5 只策略 / V15 = 2 条 / V14 = 0 条 / V13 = 0 条 ⇒ V12 是当时唯一"可实盘"的，
  尽管它的 Σtop1 远低于 V14/V15。**V14/V13 的高 Σtop1 是 top-1 现象，落到 5 只组合上几乎全负。**

**R20. 越往交易层，效应越不稳。**
- 同一次 A/B（225 vs 306 特征）：IC 层 48 组配对 **40/48 为正、Δ 均值 +0.0039**（稳定但微弱）；
  Σtop1 层同一批特征两个头往**相反方向大幅**走而 **IC 几乎没动**；策略层集成 +22.7%→+129.4%，
  **单折复现不出来**。
- 附带方法论：**配对比较（同折/同种子/同窗）比"折间跨度"那套阈值灵敏得多**，
  但绝对量级很小时只能定性。

### 8.5 工程纪律

**R21.「只重定向一半」是最危险的 bug 形态 —— 不报错、不崩，只是把东西放到别处。**
- ① 加了「输出目录」这类参数，必须**从参数入口一路追到最后一个 `write`**；
- ② **重建/覆盖任何非可再生数据之前先复制一份**；
- ③ **冻结标记保护不了"另一条写入路径"** —— 闸门只在 `prepare()` 开头拦"原地重建"，
  而 `build_year()` 是从内部被调用的，「**闸门没被绕过，是根本没经过**」。
- ⇒ 三条已进 [TODO.md](TODO.md) §12 操作纪律。

**R22.「两个重活不并行」（训练 × 分析也算）。** —— 数值待按新体积重测
- 2 折 × ~38 GiB + 一个正在跑的分析 = 顶穿 `memory.high` ⇒ OOM-Kill。

**R23. 跨池 / 跨特征集不可比；且换池会改掉每一个特征值。** —— **定论**
- `factors` 存的是 `rank` / `1−rank`，即**逐日截面内**的相对量 —— 池子从 3484 缩到 2115 ⇒
  同一天的排序分母换了 ⇒ **每个特征值都变了** ⇒ 旧权重、旧打分、旧榜单**全部作废**
  （不是"重跑 analysis 就能续上"，而是必须重训）。
- ⇒ 已升级为 [SPEC.md](SPEC.md) §3 的全局纪律：**任何一次上游重算都会让下游结论作废**。

**R24. 两个工具口径闸门。** —— 工具纪律
- ① 跨池混榜：两个工具都按 `trainingdata/meta.json` 的 `panel_digest` **强制同池过滤**；
- ② **策略矩阵只跑了主腿标签 ⇒ 非主腿的行若照拿 `ens:<头>` 的 cell，那一列显示的是主腿的策略收益**
  （曾出现 `ridge_w` 的 3d/5d/10d 三行显示同一个来自 20d 的数）—— 一个**静默的数字串行**。

### 8.5b ★ 2026-09-21 凌晨新增（策略层台地 · 名次剖面 · 全轮平均）

**R25. 同配方、同种子、同数据，只换设备（GPU↔CPU）就会换掉整个模型 —— 而 IC 看不出来。** —— **定论**
- 实测：V17 = V16 的配方一字不改，只在**这台没有 GPU 的机器**上重训（`torch.cuda.is_available()=False`，
  驱动 12.8 对不上 `torch 2.14.0+cu130`）。结果：同折 best epoch 差 1、逐格打分最大差 **6.6~8.3**；
  四折 IC 合计 **0.0391 → 0.0389**（几乎不动）、ICIR 0.328 → 0.325，而 **Σtop1 从 +0.6679 变成 −0.1635**、
  top1 稳定性 +0.086 → −0.024（**反号**）。
- ⇒ ① **任何跨版本比较必须同设备**，否则比的是浮点噪声；② 反过来说，**IC 是稳的、Σtop1 是抽签的** ——
  这给 R17/R18 补上了"同一份配方"这个最干净的对照组（此前只有"换种子"与"换折"）。
- ⚠️ 当前机器**没有可用 GPU**：驱动 570.124.04（CUDA 12.8）vs 环境里的 `torch 2.14.0+cu130`。
  没动共享环境（换 torch 版本的风险大于收益）；CPU 25 核下单折 ~9 分钟，够跑实验。
- ★★ **补测（当天稍晚，同设备同配方）**：V17 与 V22 的 `valid_predictions_by_epoch.npy`
  **16 折逐位完全一致**（最大逐格差 `0.00e+00`）—— V22 的配方只改了"打分怎么聚合"、
  没动训练，于是把这件事变成了一次干净的对照。⇒ **同设备是完全可复现的（逐位）**；
  V16↔V17 的差异**可以确定地归因到设备**，不是随机噪声。
  ⇒ 使用建议：**同设备内做 A/B 不需要重复跑**；跨设备（换机器/换镜像）**必须重跑全部候选**。

**R26. 策略层能站住的不是"最好的参数"，而是**持有期 + 分散度**那片台地。** —— **定论（在 2025Q3~2026Q2 评价窗上）**
- 网格 3000+ 格（`V17/strategy_scan.py`，`quick`/`focus`/`plateau` 三档）实测：
  - **峰值格是噪声**：`re800_t5_h2`（集成 +58.5%）四折最小值 −9.0%；`re1200_t5_h1`（集成 +24%）四折最小 −28.6%。
    集成收益高、四折最小值却很差 —— 典型的组合路径抽签。
  - **台地是真的**：`topn=5 + min_hold=5 + exit_rank 400~700`、**skip=0** 时，
    集成净收益 **+5.9%~+15.9%**、**四折最小值 −4.5%~+7.3%**、三种集成规则（相加/秩平均/中位）**全为正**。
  - 对照 baseline `top1_1d`：集成 **+28.07%** 但**四折全负**（−14.7 / −16.8 / −43.8 / −52.8）。
- ⇒ 写法上要引 **R19**（晋级看"四折全正条数"）：这一族是**拿 12~22pp 的均值换掉尾部风险**，
  且**费用从 25,514 元降到 814~1,213 元**（10 万本金）—— 后者是**唯一确定性**的效应（不受路径抽签影响）。
- ★ 注册表里只放台地邻居（400/500/600/700 四个），**不放峰值格**。

**R27. 「跳过第 1 名」在名次剖面上不成立 —— 它带来的收益是组合路径噪声。** —— **定论**
- 集成打分的**第 1 名日均 1d 收益 +0.276%/天（t=1.33）**，是 1~6 名里**最高**的；`skip=1` 的网格里
  确实成片更好（配对平均 +10.2pp、48/72 格），但**四折最小值常常更差**（如 `re800_t5_h2_sk1`：
  集成 +58.5%、四折最小 −9.0%），而 `ranked[1:]` 与 `ranked[:]` 的前 5 名期望收益几乎相同
  （+0.133% vs +0.125%/天）⇒ **差异只能来自组合路径**。
- ⇒ `skip` 只作**对照行**留在注册表里，不当结论。★ 这条同时是"**先看名次剖面再信网格**"的方法论。

**R28. 四折集成的 top-1 是真信号，而且比任何单折的 top-1 都好。** —— **定论（本数据）**
- 名次剖面（`V17/rank_profile.py`，评价窗 242 天、已套次日开盘可买掩码）：
  **集成** top-1 **+0.276%/天**；四折各自 **+0.163 / +0.149 / −0.087 / −0.146**（均值 ≈ +0.020）。
  集成 top-1 的集中度也过关（89 只票、最大单票占比 7.4%、前三 20.7%）。
- ⇒ **与 R16 的旧结论相反**（那条说"等权混出的 top-1 是死位"）—— 差别在**口径**：
  R16 测的是旧引擎上的"**等权混合**"，这里是用户 2026-09-20 定的"**四折打分直接相加**"。
  ⇒ 引用时务必分清"相加"与"等权归一再平均"。
- ⚠️ t=1.33 单看**不显著**；它靠的是"四折方向一致 + 集中度过关 + 跨季度不靠单季"这三条撑住。

**R29. ★ 选轮：**不选**比选好 —— 全部轮次的预测取平均，远好于按 val_wei 挑一轮。** —— **定论（本数据，需多种子复核）**
- 同一份 V17 训练（16 折）上，13 条选轮规则同台（`V17/scan.py`，全部只用验证窗信息、在评价窗上打分）：

  | 规则 | IC | ICIR | Σtop1 | 剔最好5日 | top1_1d 净收益 |
  |:--|--:|--:|--:|--:|--:|
  | `wei100_0.7`（V16 现行） | 0.0389 | 0.325 | **−0.164** | −0.660 | −40.7% |
  | `wei100_0`（只用收益） | 0.0397 | 0.330 | −0.228 | −0.799 | −44.1% |
  | `ic`（只用 IC 挑一轮） | 0.0398 | 0.333 | +0.672 | −0.035 | +32.8% |
  | `rankic` | **0.0420** | 0.322 | +0.518 | — | +15.3% |
  | `medianall`（全轮中位） | 0.0370 | 0.328 | +0.702 | — | +33.4% |
  | `avgrecency`（近轮加权） | 0.0374 | 0.332 | +0.857 | — | +50.6% |
  | **`avgall`（全轮平均）** | 0.0381 | 0.321 | **+1.185** | **+0.477** | **+113.2%** |

- **单调性**：平均得越多越好（avgall +1.185 > avg2on +1.076 > avgrecency +0.857 > medianall +0.702 >
  单轮最优 +0.672 > 现行 −0.164）⇒ 不是尖刺，是**方差削减**这一个机制。
- **配对检验**：avgall 对现行规则在**四折上 4/4 更好**；**剔最好 5 日仍 +0.477**（现行规则 −0.660，
  `ic` −0.035）—— 本库 R4 的集中度闸门只有 avgall 过得去。H1/H2 = +1.02 / +0.17（双正）。
- **机制**：与 R28 同一条 —— 头部 alpha 在"**模型之间的共识**"上，不在任何单轮的尖峰上。
  `val_wei` 里 93% 是"可执行 top5 的 5d 收益"（样本 5 只的噪声），所以**它挑出来的那一轮本来就近似随机**。
- ⇒ 已落成 **V22**（`epoch_agg='mean_all'`）。⚠️ 仍需多种子复核：它是 13 条规则里的最好一条，
  存在多重比较风险（虽然单调性 + 4/4 配对 + 集中度三项都支持它）。
- ★★ **换种子复核（V23 = V22 只改 `seed` 3253→3254，在 2026Q1 上重训四折）**：

  | 规则 | IC | Σtop1 | top1_1d 净收益 |
  |:--|--:|--:|--:|
  | `wei100_0.7`（选轮） | +0.0278 | −0.1137 | −24.56% |
  | **`avgall`（全轮平均）** | +0.0249 | **+0.1551** | −4.48% |
  | `avg3` | +0.0253 | +0.2022 | +0.30% |

  ⇒ **方向复现**：`avgall` 的 Σtop1 比选轮高 **+0.269**（换一份种子、换一段窗，方向一致）。
  ⚠️ **但要写清代价**：这次 `avgall` 的 **IC 低 0.0029**（0.0249 vs 0.0278）——
  与 V17 那次（IC 只低 0.0008）合起来看，**全轮平均是"用很小的 IC 换很大的 top1"**。
  这也解释了为什么它在本库"IC 优先"的天梯榜上**不会自动排到第一**（见 LEADERBOARD.md）。
- ⚠️⚠️ **第三次复核（V26 = 3d 标签、seed 3254、全量 242 天）翻出了一条重要的边界**：

  | 规则 | IC | ICIR | Σtop1 |
  |:--|--:|--:|--:|
  | `wei100_0.7`（选轮） | +0.0413 | 0.383 | −0.0892 |
  | `avgall`（全轮平均） | +0.0412 | 0.362 | +0.0725 |
  | `ic`（按验证 IC 选一轮） | +0.0411 | 0.359 | +0.7055 |
  | **`last`（取最后一轮）** | +0.0381 | **0.394** | **+1.3901** |

  ⇒ ① **"不选 > 按 val_wei 选"这条复现了 3/3**（三次里 `avgall` 都高过 `wei100_0.7`）；
  ② **但"全轮平均是所有规则里最好的一条"只在 5d 配方上成立** —— 3d 上 `last` 远好（+1.39 vs +0.07）。
  **规则的好坏随配方翻转**，所以"在测试窗上挑规则"本身就是选择偏差（R35）。
  ③ 之前那三条支撑（单调性 / 四折配对 / 名次剖面）**只对 V22 这个配方成立**，别外推。

**R30. ★ 头部质量随「模型份数」单调改善，而且到 4 份**还没饱和** —— 加模型是本数据上最稳的杠杆。** —— **定论（本数据）**
- 把四折（每折内部已做全轮平均）当模型池，枚举所有 k 子集（`V17/ensemble_size.py`）：

  | 模型份数 k | 组合数 | IC | Σtop1 均值 | Σtop1 区间 | ICIR | 剔最好5日 |
  |--:|--:|--:|--:|:--|--:|--:|
  | 1 | 4 | +0.0350 | +0.323 | [−0.084, +0.728] | 0.303 | −0.265 |
  | 2 | 6 | +0.0370 | +0.764 | [+0.359, +1.446] | 0.316 | +0.082 |
  | 3 | 4 | +0.0378 | +0.950 | [+0.758, +1.119] | 0.320 | +0.276 |
  | **4** | 1 | **+0.0381** | **+1.185** | — | **0.321** | **+0.477** |

- **四项指标全部单调改善**，连最苛刻的"剔最好 5 日"也从 **−0.265 翻成 +0.477**；
  同时**组合之间的离散度随 k 收窄**（k=2 跨度 1.09、k=3 跨度 0.36）—— 就是方差削减。
- ⇒ ① **与 R13 相反**（那条说"集成能提 IC 但会拉低 Σtop1"）：R13 测的是**旧引擎的等权合并**，
  这里是"直接相加 + 折内全轮平均"，两回事，引用时必须写清口径。
  ② **曲线还没饱和 ⇒ 加模型（8 折 / 多种子）是下一步最该花算力的地方**，且它同时改善
  IC 与集中度，不像别的改动是此消彼长。
  ③ 附带一条：k=1 时四折的 Σtop1 区间 [−0.084, +0.728] 就是"**单种子纪录 = 种子中奖**"（R18）
  在这份数据上的直接读数。
- ★ **边界条件（当天实测，`V17/grow_ensemble.py`）："加模型"只在模型**同质**时成立。**
  一个看似免费的做法是"把更早季度的模型组也平均进来"（它们同样没见过这一天）——
  在 V22 上实测：模型份数 4.0 → 9.8，**IC 0.0381 → 0.0382（不动）、Σtop1 +1.185 → +1.127（略差）**；
  在 V17 的弱打分上则是 IC 0.0389 → 0.0366（**明显变差**）、Σtop1 −0.164 → −0.014（变好）。
  ⇒ **早季组训练数据更少、分数更弱**，它们的"共识"是往弱模型那边拉。加模型前先问：
  新加的那批是不是**同档次**的？
- ★★ **曲线不能外推（V24 实测）：8 折比 4 折**差** —— 因为新加的折彼此更相关。**
  V24 = V22 只把 `folds` 4→8（每折训练数据从 3/4 提到 7/8、验证块从 ~450 天缩到 ~243 天），
  结果 **Σtop1 +1.185 → +0.946**、稳定性 +0.141 → +0.111（IC 0.0381 → 0.0383，基本不动）。
  机理直接测出来了（同一段评价窗、逐日截面秩相关）：

  | 单元 | 折数 | 折间两两秩相关 均值 | 区间 |
  |:--|--:|--:|:--|
  | V22 | 4 | **0.728** | [0.678, 0.781] |
  | V24 | 8 | **0.808** | [0.785, 0.836] |

  ⇒ **8 折里任意两折共享 7/8 的训练数据（4 折只共享 3/4）⇒ 它们不是"更多独立模型"，
  只是同一批模型的更细切分。**

- ★ **但"加相关模型没用"这句话要留余地 —— 三类模型的相关系数实测都在 0.70~0.81 之间**，
  差别并没有大到能干净地把结果分开（同一天、同一段评价窗，逐日截面秩相关）：

  | 加模型的来源 | 两两秩相关 | 加进去之后 Σtop1 的变化 |
  |:--|--:|:--|
  | 同一折的**不同轮次**（V22 全轮平均） | **0.700 ~ 0.776** | 大幅变好（−0.164 → +1.185）|
  | 同一单元的不同**折** | 0.728 | 随份数单调变好（k=1→4：+0.323→+1.185）|
  | 同切分、不同**种子**（V22 vs V23） | 0.765 | 基本不动（Q1：+0.228/+0.253 → 合并 +0.240）|
  | 切得更细的**八折**（V24） | 0.808 | 没变好（+1.185 → +0.946）|
  | 不同**训练标签**（V22 的 5d vs V19 的 3d） | **0.954** | 拖回中间（IC 0.0381→0.0394、Σtop1 +1.185→+0.621）|

  ⇒ 三个读数要一起说：① **"份数"不是充分变量**（同相关系数下有的加得动、有的加不动）；
  ② V24 的 Σtop1 下降**本身也可能是抽签**（同一批候选的 IC 只差 0.0002、ICIR 只差 0.004，
  而 Σtop1 单折区间就有 [−0.084, +0.728]）—— **别把"没帮上忙"写成"加折有害"**；
  ③ **目前唯一被四重证据支持的做法仍是"同一份训练内多轮平均"**（R29/R31），
  其余加模型的路子都还只是"试过、没看到效应"。

**R31. 全轮平均把第 1 名从"还行"变成"显著" —— 名次剖面本身也是验收工具。** —— **定论（本数据）**
- 同一口径的名次剖面（`V17/rank_profile.py`，已套次日开盘可买掩码、242 天），集成的第 1 名日均 1d 收益：

  | 打分口径 | 第 1 名 | t | 剖面形状 |
  |:--|--:|--:|:--|
  | V16 `best.pt`（按 val_wei 选轮） | +0.276% | 1.33 | 第 1 名**不是最高**（第 5 名 +0.389%、第 10 名 +0.359%） |
  | **V22 全轮平均** | **+0.490%** | **2.19** | 第 1 名**是最高**（次高第 5 名 +0.267%） |

- ⇒ ① 一个"排序模型"的名次剖面**本该在头部单调**；V16 那种"第 1 名不如第 5 名"的形状本身就是
  "打分口径有问题"的直接证据 —— **网格扫描（R26/R27）与选轮扫描（R29）都可能被组合路径骗过去，
  名次剖面不会**。以后任何打分口径改动，先看这张剖面。
  ② t=2.19（p≈0.03）单看**不足以**扛多重比较，它靠 R29 的单调性 + R30 的曲线 + 四折配对一起撑。

**R32. 训练标签的期限：**3d 在 IC/ICIR 上最好**（全量已跑完）。** —— **定论（本数据）**
- 做法（`V17/screen.py` + `V17/screen_train.py`，RD9）：标签变体（V18=1d / V19=3d / V20=10d /
  V21=20d）**先只训 2026Q1 的四折**（每档 ~10 分钟）筛形状，再让最好的一档跑满四个季度。
  打分口径与 V22 相同（全轮平均），所以比的只有"训练标签"这一个变量。

  **筛（2026Q1，56 天）**：

  | 训练标签 | IC | ICIR | Σtop1 | 稳定性 |
  |:--|--:|--:|--:|--:|
  | 1d（V18） | +0.0466 | 0.446 | +0.155 | +0.044 |
  | **3d（V19）** | **+0.0507** | **0.476** | **+0.328** | **+0.164** |
  | 5d（V22，对照） | +0.0487 | 0.464 | +0.228 | +0.116 |
  | 10d（V20） | +0.0458 | 0.474 | −0.002 | −0.001 |
  | 20d（V21，仅 1 折） | +0.0444 | 0.436 | −0.243 | −0.171 |

  **全量（V19，242 天）**：IC **+0.03993**、ICIR **0.3415** —— **两项都是全库最高**
  （V16 +0.0391 / V22 +0.0381 / V24 +0.0383；ICIR 0.328 / 0.321 / 0.317），
  且**四个季度的 IC 全部高于 V22**；但 Σtop1 只有 **+0.1168**（V22 是 +1.1849）。
- ⇒ ① **期限是单峰的，3d 是峰**（3d > 5d > 1d，10d/20d 明显变差）—— 与参考工程
  "冠军是 1d/3d 短周期头部"一致，也**修正了 RD9 的直觉**：1d（训练目标与交易动作同期限）
  并不自动更好。
  ② **它是 R6 的又一个实例**："IC 最高的头"和"Σtop1 最好的头"不是同一个 ——
  V19 赢在稳的两个指标上、V22 赢在抖的那个上。**把两者平均起来没用**（打分秩相关 0.954）。
- ⚠️ **IC 的优势（+0.0018）在量级上仍然很小**（R9 折间跨度 ±0.03~0.09）；它靠的是
  **四个季度方向一致** + 配对比较，不是单看一个数。**换种子复核（V26）在跑，结果出来前别采信。**

**R33. RD2 的"尾部池验证集"（方案 B）没有观察到明确收益。** —— **未观察到效应（本数据）**
- 做法（V25 = V22 + `split_scheme='tailpool'`，实现落在 `V25/model.py::splits()`）：
  **训练集四折相同**（= 尾部池起点之前的全部历史，1488 → **1743** 个交易日，+17%）；
  验证集从**紧贴测试窗的 243 个交易日尾部池**里随机抽 1/4（四折并集 = 整个池）。
  这正对应用户 2026-09-20 提的两条硬目标（训练数据尽量多 + 验证集贴测试窗）。
- 实测（对照 V22，配方只差切分）：

  | | IC | ICIR | Σtop1 | 稳定性 |
  |:--|--:|--:|--:|--:|
  | V22（全历史四段轮换） | +0.0381 | 0.321 | **+1.1849** | **+0.141** |
  | V25（尾部池） | +0.0384 | **0.334** | +0.4899 | +0.061 |

**R33（续）** ——
- ⇒ **IC 只 +0.0003（噪声量级）**，但**四个季度的方向不一致**（Q4/Q1 高、Q3/Q2 低）⇒ 判为未观察到效应；
  ICIR +0.013 是唯一像样的改善（IC 的均值没动、标准差小了）；Σtop1 掉 0.70 但**落在单折区间
  [−0.084, +0.728] 内**，同样不能当"切分有害"的证据。
- ★ **方法论价值大于数字**：这一步同时把"训练数据 +17%"与"验证集贴测试窗"两个**看起来很对**的
  改动都测了，**结果是没有明确收益**。⇒ 以后"理论上更好"的切分改动，同样要先跑对照再说。
  （另：RD2 本来最担心的是"尾部池单折验证只有 61~243 天、早停噪声大"，实测中**没有**看到
  早停质量塌掉的迹象 —— 因为 V22 起打分已是"全轮平均"，选轮噪声对最终打分的影响被压掉了。）


---

**R34. ★★ IC 与 top 收益在"训练标签的期限"上是**同一个旋钮的两端** —— 换不来两头。** —— **定论（本数据，两份种子各复现一次）**
- 把 3d 与 5d 两份打分**逐日截面秩标准化后**按权重线性组合（`V17/horizon_ensemble.py` 同族口径；
  秩标准化是必须的 —— 两个版本的打分尺度不同，直接相加会让大尺度那方主导）：

  | 3d 权重 w | IC | ICIR | Σtop1 | 剔最好5日 | top1_1d 净收益 |
  |--:|--:|--:|--:|--:|--:|
  | 0.00（纯 5d，V22） | +0.03813 | 0.321 | **+1.1849** | **+0.477** | **+113.2%** |
  | 0.25 | +0.03882 | 0.326 | +1.0350 | +0.308 | +83.1% |
  | 0.50 | +0.03936 | 0.331 | +0.9401 | +0.213 | +63.6% |
  | 0.75 | +0.03972 | 0.336 | +0.7514 | +0.092 | +37.6% |
  | 1.00（纯 3d，V19） | **+0.03993** | **0.341** | +0.1168 | −0.514 | −31.2% |

- **两头都是单调的**：w 每加 0.25，IC 涨 ~+0.0006、Σtop1 掉 ~0.25。**没有"中间那个两头都不差"的权重。**
- ✅ **这个权衡是复现过的**：换一份种子（V26 = V19 只换 seed）得到 IC **+0.04117** / ICIR **0.3624**
  （比 V19 还高）× Σtop1 +0.073（同样很低）—— **3d 的两份种子在 IC 与 ICIR 上都高于 5d 的两份**
  （V19/V26：0.0399/0.0412、0.341/0.362 vs V22/V16：0.0381/0.0391、0.321/0.328），
  **而 top 收益反过来**。⇒ 不是单次抽签，是**标签窗口本身的性质**。
- ⇒ **给决策的一句话**：**"要 IC 还是要 top 收益"必须由用户拍板** —— 本库的天梯榜按"IC 优先"
  排序，于是它会**自动推荐 3d 那一端**（top1 弱、策略层甚至是负的）；而策略层的钱在 5d 那一端。
  两者不可兼得（打分之差已经用秩相关量过：同一标签内不同种子 0.765，跨标签 **0.954**）。

---

**R35. ★★「哪条选轮规则最好」**随配方翻转** ⇒ 在评价窗上挑规则，本身就是选择偏差。** —— **方法论，定论**
- 三次同族实测（同一套 13 条规则、同一天、同一段评价窗）：

  | 配方 | 最好的规则 | 次好 | `avgall` 的位置 |
  |:--|:--|:--|:--|
  | 5d 标签 · seed 3253（V17/V22） | **`avgall`** +1.185 | `ic` +0.672 | 第 1 |
  | 5d 标签 · seed 3254（V23，Q1） | **`avgall`** +0.155 | `avg3` +0.202（Q1 上 avg3 更高） | 第 1~2 |
  | **3d 标签 · seed 3254（V26，全量）** | **`last`** +1.390 | `ic` +0.706 | **第 3**（+0.073） |

- ⇒ ① **只有"不选（平均）优于按 val_wei 选"这一条是 3/3 稳的**；
  ② **"平均是不是最好"不稳定** —— 换个标签就翻。
  ③ 因此本库对规则的做法必须写死：**先在某个配方上按"四折配对 + 名次剖面 + 集中度"选规则，
  然后把它固定进 `RECIPE`（进 run_id）**；**不许**在评价窗上逐配方挑规则再报数 ——
  那就是把 test 当验证集用，本库在 `best/` 与榜单上反复声明过这条纪律。
  ④ 反面读数同样重要：V26 的 `last` 拿到 Σtop1 +1.39、ICIR 0.394 也最高，
  **但它的 IC 是三次里最低的（0.0381）** ⇒ 又一次印证 R6（IC 与 top 收益不是同一个东西）。

**R36. ★ RD6 删稀疏/离散因子：**对 IC 无效应，但在 3d 配方上稳定把 top 收益抬起来（3/3 配对）。** —— **定论（本数据，3 粒种子 × 2 个标签）**
- 做法（V27/V28/V29/V30）：按**数据可得性**删因子（判据只用 2018-01-02~2025-06-30，严格早于测试窗）——
  非空率 < 0.5 **或** 日均唯一值 < 50 就删。**337 → 302 个特征**（`V27/features_rd6.json`、
  `V27/select_features.py`）。删掉的 35 个正是用户点名的那两类：低覆盖（`dragon_tiger_org_net_20` 4.7%、
  `top_list_net_rate_20` 10.3%、`seal_*_20` 16.4%、`pegh5` 47.1%）与事件型/近离散
  （`consecutive_limit_up` 日均唯一 1.6、`limit_down_event_5` 2.2…）。
- **判据：配对比较**（同一配方、同一折、同一段窗，**只差"输入列"这一个变量**）：

  | 配对 | 标签/种子 | 337 列 IC | 302 列 IC | ΔIC | 337 列 Σtop1 | 302 列 Σtop1 | **ΔΣtop1** |
  |:--|:--|--:|--:|--:|--:|--:|--:|
  | V22→V27 | 5d / 3253 | +0.03813 | +0.03941 | +0.00127 | +1.1849 | +0.5289 | **−0.6560** |
  | V19→V28 | 3d / 3253 | +0.03993 | +0.04137 | +0.00144 | +0.1168 | +0.3127 | **+0.1959** |
  | V26→V29 | 3d / 3254 | +0.04117 | +0.04067 | −0.00051 | +0.0725 | +0.5305 | **+0.4580** |
  | V31→V30 | 3d / 3255 | +0.04100 | +0.04067 | −0.00032 | **−0.1625** | +0.4538 | **+0.6162** |

- ⇒ ① **IC：三对 3d 里 1 正 2 负、均值 +0.0002 ⇒ 无效应**（5d 那对的 +0.0013 不足以撑起来）。
  ② **Σtop1：3d 配方上 3/3 为正、均值 +0.42**，而且**它修掉的是 3d 配方最大的毛病** ——
  337 列的 3d 版本 Σtop1 是 +0.117/+0.073/**−0.163**（散、可为负），302 列是
  +0.313/+0.531/+0.454（**三粒种子全正、都在 +0.3 以上**）。
  ③ **5d 配方上是反例**（−0.656）⇒ **不能写成"RD6 总是好"，要写成"RD6 让 3d 配方的 top1 不塌"**。
- ⇒ **可用的配方结论**：想要 IC，用 3d；**想要"IC 高且 top1 不塌"，用 3d + RD6 清单**
  （V28/V29/V30 三粒种子：IC 0.0407~0.0414、Σtop1 +0.31~+0.53）。
- ★ **过程本身是一条方法论**：第一版结论（"IC 四季度全变好"）来自单种子，第二轮复核（V29）
  把它推翻成"无效应"，**第三对（V30/V31）才把真正稳的那一半（top1）分离出来**。
  ⇒ **2 粒种子不足以判一个改动，尤其当两个指标的读数方向不一致时。**
- ⚠️ 附带：本版暴露了一个真实的静默 bug（配方声称 302 列、实际用 337 列训练），
  被 `--audit` 的权重形状检查抓住 —— 见 §11-26。

## 8.6 只在这一份文档里记录过的实测（原 VERSIONS.md §8.6）

★ 逐版叙事删掉之后，下面这些**别处没有**，所以单独留档。它们都是**机理认知**，不是版本数字。



**U1.「尾部加权按原始收益排序」有副作用：数学上等于提高涨停股的拟合权重。**
V3 的损失配方让**线性模型**短期限 IC 转负（同折同标签 V2b +0.0501/+0.0401/+0.0725 →
V3 **−0.0288/−0.0144/+0.0013**），特征重要性上 `consecutive_limit_up` **从第 8 位升到第 1 位**。
机制：A 股当日截面里已实现收益最高的 20% 中，涨停股被系统性过度代表；`rank_gauss` 又把极端收益
压到 ±4 ⇒ 模型注意力从"温和地排序全截面"转向"把涨停/跌停事件特征排明白"。
**★ 衍生方向**：尾部加权的 `order_by` 值得单独实验（换成"次日可执行收益"或"剔除涨停后的收益"）。
⚠️ [SPEC.md](SPEC.md) §6.5 只写了设计意图，**没记这个副作用**。

**U2. 掩码可以只用 `trainingdata` 造出来，不依赖价格层、PIT 安全。**
- T 日停牌 = 特征覆盖率 < `min_feature_coverage`（停牌格 **98.44% 特征全空**；覆盖达标格次日买不进
  仅 **0.70%**）；
- T 日收盘涨停 = `consecutive_limit_up` 的当日截面 **rank ≥ 0.98**（**精确率 99.6% / 召回 91.4%**，
  真实涨停格占 1.795%）。该特征在涨停行均值 **0.969** vs 非涨停 **0.491**、**AUC 0.988**；
  且 rank 是**逐日截面内的相对量，天然自校准，换年份不用改阈值**。
- ★ **在价格层已被删、Σtop1 暂时出不来的当下，这条给出一个不碰价格层的备选判据。**

**U3.「陈旧数据比缺失数据更危险」。**
曾把归因判错：换成 `min_feature_coverage=0.2` 后，88.8 万个格子里**只有 2 个**的有限打分被改掉、
Σtop1 一位数字都不变 ⇒「闸门修紧是正确性改进，但不解决问题（实测收益≈0）」。
真正原因是**数据陈旧**：1,196 个覆盖率达标的格子，价格/成交量类因子停在上一个交易日，
树模型在"陈旧的 rank 值"上给出极端分值（0.13% 的格子被选中 top1 达 6.7%，**50 倍**）。
⇒ **修闸门 ≠ 修问题**。

**U4. 数据重建的等价性是巧合，不是保证。**
- 事实：上游 230 个旧因子目录的文件在快照建立（00:34）之后的当天 **16:59~17:10 被整体重写过**
  ⇒ **即使没有事故，"在新快照上重跑旧特征"也不等于复现旧结论**；
- 实测：重建后 `ridge_w`/`xgb` 五个标签 Δ = **0.0000 逐位一致**，`nn_cons` ≤0.0003；
- ★ **"这不是没事，是我运气好"** —— 当时没有任何机制保证重建会等价，上游顺手改一个公式就是
  真的静默污染，**且不可能事后发现**；
- 「旧结论仍可复现」依赖的是 test 集上的下游指标而非逐格比对，而旧快照的 sha 列已随数据一起没了
  ⇒ **严格意义上的复现其实做不到**。

**U5.「净值与日期差一天」——又一个"不报错、静默错位"的同类 bug。**
`mx/strategy.py:_metrics` 里 `eq = np.concatenate([[capital], eq])` 给净值前置了一格「建仓前本金」，
而 `nav_dates` 只存了 m 个 ⇒ 两条序列差一天。出图只是第一个撞上的消费方，
**任何"按日对齐净值"的代码都会整体错位一天（图上看不出，数字全串）**。
修法：`nav_dates` 同步前置一格 + 用窗口前一个交易日 + `plots` 末尾对齐防御。

**U6. 种子冗余的方法论前提。**
实测确认 `mx/dataset.py:make_splits` 的切分**只由折号决定**，所以四个种子集的 train/valid/test
**逐字相同** ⇒ "这比'折间跨度'更严格：折间跨度里还混着'验证窗位置'这个因素"。

**U7. `engine_hash` 漂移的实测与不重跑判据。**
四个折记的是**四个不同的引擎**（`a8f667983752ba76` / `e51887fdcabc04f5` / `437fd34265175f42` /
`5ebbccd9264dd024`）⇒ 活跃开发期续跑半截折，天然产出"跨引擎混合折"。
技法：用 `.pyc` 头部存的「源文件 mtime + size」与当前 `.py` 对比，能**在不重跑的前提下**
判断哪些模块被改过。

**U8. 两个 shell 坑。**
① `setsid nohup bash -c '...'` 里的中文 heredoc 被吞成 `?`（watcher 的
`grep "V2b 阶段结束"` 变成 `V2b ????????????`，永远匹配不上）；
② **`${#FOLDS_LIST}` 是字符数不是折数**（`"1 2 3 4"` 长度 7 ⇒ `train.sh` 报"4/7 折成功"）。

**U9. V1 的 83 因子 bug 证据链。**
`V1/logs/fold1.log` 记"83 个因子已按方向翻转"；ridge 的 |coef| 中位数 **0.004**、
171/224 个系数 < 0.01；lgbm `best_iteration = 4`（500 棵树只走 4 轮）。
⇒ 机理：`clip(0.001,0.999)` 作用在 `−rank`（值域 [−1,0]）上，把整列压成常数 0.001。
修法与影响面见 [SPEC.md](SPEC.md) §3「三个口径」第 2 条。

---

## 8.7 参考工程复刻（V13/V14）的三条留存

- **复刻没有造出更好的东西** —— 两个复刻单元都没进 `best`：V14 `nn_wpcc` Σtop1 +0.393 但
  "≤5 只策略四折全正" **0 条**；V13 `nn_v8_nw` 同样 0 条、多数策略为负。
  `nn_wpcc` 的高 Σtop1 是 **top-1 现象**（第 1 名 +0.167%，2~5 名均值仅 +0.010%，
  与 `nn_cons`"信号铺在第 2~6 名"形状相反）。
  ⇒ **与数据无关的设计观察**：Σtop1 高但集中在某一个名次上，落到 5 只组合就没了。
- **`wpcc` 损失有保留价值**：按**预测值的降序名次**给权重 `w_i = 0.5^((i−1)/(n−1))` 再算加权 Pearson
  —— "排在前面的那些票，相关要算得准"。它与本库的尾部加权**方向相同、依据不同**：
  我们按**真实收益**排序取头部（副作用见 **U1**），它按**预测**排序 ⇒ 不依赖真实收益的分布。
- **修正 `soft_rank` 方向不是"无害纠错"**：复核实测原式 `ic+rankic` 在真实 IC 量级下**近乎相消**
  （+0.033 + (−0.037) ≈ −0.004，甚至反号），修正后 `−3.0·(ic−rankic)` 的**梯度约为原式的 7 倍**。
  ⇒ 与参考工程的数字对比时，**线性头的有效学习信号强度不是同一个量**。
  ★ **权威版本是 `V16/mx/models/nn_ref.py` 的 docstring**（10 条偏离表），以它为准。
- ⚠️ V14 的复刻把 `MultTime2dMixer` 与 `KAN` 换成了 `Linear(128,1)`（源码不可得）
  ⇒ **它的成绩不是 KAN 模型的成绩**，引用时别混淆。

## 9. 已确认的事实（带证据）

> ⚠️ 本表里凡带**具体数字**（因子个数、覆盖区间、行数）的，都是 **2026-09-20 之前**在旧数据上
> 实测的，其中"上游有 230 个因子、覆盖自 2012-01-04"这类**已经变了**（现在是 342 个产物、
> 覆盖自 2018-01-02）。**方法论层面的结论（怎么判断覆盖不一致、为什么要用产物端点而不是日历）
> 仍然成立**，具体数字引用前请重新核对。

| 事实 | 证据 |
|:--|:--|
| 上游 230 个因子的**覆盖区间并不一致（是"起点晚"，不是"中间缺天"）** | 实测起点分布：**190 个从 2012-01-04**（日期数恰好 3573 = 全跨度日历天数，一天不缺），**40 个晚起点**（最大一簇 28 个从 2018-01-02，是 `growth_stability`/`revenue_cagr_3y` 这类财务因子）<br>末尾**已一致**：230/230 都到 2026-09-17（2026-09-17 深夜实测；当天早些时候曾见到 89/141 参差，那是因子任务正在写盘的中间态） |
| 票池口径与模块② **天然一致，不需要再存一块** | 删掉 `universe` 块之前实测：初加工调模块② `Engine.universe_for` 得到的掩码，在 4,475,340 行里 `in_universe=False` **0 个**。原因是轴就取 `Engine.codes`，掩码必然全 True（冻结口径「主板 ∩ 至今存续 ∩ 从未ST ∩ 上市≤2018-01-01」保证了这点）⇒ **恒等块，没有信息**，2026-09-20 下午随四块收敛删除。（旧记录里"池内占比 0.8756"是 3484 只池时代的数据，已作废） |
| 标签**未做 universe 掩码**（这条**仍然成立**，别因为删了 `universe` 块就忘掉） | 池外有值 24,883 格、池内无值 18,308 格 ⇒ 样本必须取 `池内 ∩ 标签非空 ∩ 特征覆盖达标`。★ 现在轴本身就只剩池内 2115 只，所以"池内"这一项**恒真**（恒等化简），但约束的**意图**没变 —— 改 `data.py::sample_mask` 时不要把整条约束一起当废码删掉 |
| 交易日历**含未来 16 个交易日** | 到 2026-10-16 ⇒ 日期轴一律取产物实际日期，禁用日历 |
| **折 = 种子对线性模型无效** | V1 首跑 ridge 四折 test RankIC 完全相同 ⇒ 改为**验证窗轮换 × 种子** |
| 日换手是策略杀手 | 基线 top1_daily 日均换手 130~141%，成本拖累 1.6~2.8%/月 |
| 强单因子基线 | 模块② 最强 `idio_vol_60` 的 RankIC ≈ \|0.074\| |
| LightGBM 官方 wheel 是**纯 CPU 版** | `device='gpu'` 别想当然；GPU 的价值在 PyTorch NN |
| **样本的特征覆盖率门槛取 0.2（`data.min_feature_coverage`）** | 实测**逐日**均覆盖：2012 ≈ **0.43**、2014 ≈ 0.45、2020 年代 ≈ **0.83**（早年偏低是 40 个晚起点因子 + 长回看因子早年无值所致）。门槛的作用是剔"近乎全空"的行，所以压低到 0.2 —— 设 0.5 会把 2012~2015 整片切掉 |
| ★★ **不算"买不买得进"的 top_ret 是假的** | 实测 V3 折 1（test 窗 253 天）：`gbdt_w/label_ret_5d` 的 top1 有 **37.9% 的交易日次日一字涨停/停牌买不进**；那些天平均 **+3.49%**，买得进的天只有 **+0.18%**。不套掩码 Σtop1 = **+3.777**，套上（正确对齐的）掩码后 = **−1.576** —— **"收益"全是买不到的**。模型会自发学会"挑明天一字板"（涨停在动量/量比/涨停计数这些特征里是可见的）。⇒ 主判据必须套 `entry_ok`（见 [SPEC.md](SPEC.md) §7）<br>★★ **2026-09-18 修正**：本行原有的 "从 +3.638 掉到 +0.289" 是**错位口径**下测的（见 [LESSONS.md](LESSONS.md) §8.5），已作废；正确口径下是**负的** |
| **折间差异可以很大，单折数字不构成结论** | V3 折 1 的 `gbdt_w/20d` top1 在 253 天里只集中在 3 只票上（603378.SH 44 次、600117.SH 23 次、600691.SH 20 次），且**去掉最好的 5 天后 Σ 转负**（+0.248 → −0.327）—— 典型的彩票型结果，正是用户要求做种子冗余的那类 |
| **GPU 已就位，但 LightGBM 吃不到** | `torch 2.14.0+cu130` 可用 CUDA；`xgboost 3.4.1` 带 CUDA（实测 100 棵树 2.11s vs CPU 7.15s，**快 3.4×**）。**LightGBM 官方 wheel 是纯 CPU** ⇒ 树的 GPU 路径只能走 XGBoost |
| **XGBoost 的排序目标必须用 `XGBRanker`** | `XGBRegressor.fit` 的参数表里根本没有 `group`/`qid`（实测 3.4.1 直接 TypeError），`XGBRanker.fit` 才有 |

---

## 10. 死胡同清单（参考工程验证过，**不要重试**）

| 方向 | 结论 | 对我们 |
|:--|:--|:--|
| GBDT 排序器当集成组件 | 单独弱、混入即稀释（信号近似线性） | ✅ 适用（V1 已见同向迹象） |
| 长周期标签重训（10d/20d 头） | Top 选择锐度钝化（IC ≠ Top1 利润） | ❌ **本模块已推翻** —— 见下方"★ 修正"。旧结论来自纯 IC 视角，**没把换手成本算进去** |
| 因子剪枝 | IC 升但 Top1 崩 | ⚠️ 剪枝要看**策略层**，不能只看 IC |
| 打分平滑（均线/EWM） | Top1 尖锐性就是 alpha | ✅ 适用 |
| 止损 <6% 或 >10%、移动止盈 | 鞭打或切掉赢家 | ⚠️ 先记着 |
| 收盘买入捕捉隔夜 | 隔夜对 Top1 不利 + 涨停买不到 | ✅ 适用（我们已是 T+1 开盘） |
| 阈值择时（分位数） | 低分日反而更好，全负 | ✅ 适用 |
| 联合共享躯干双头（1d+3d 同网络） | 信号互相破坏 | ⚠️ 将来做多任务网络时注意 |
| 只加种子数量（24 折） | 稀释锐度 | ⚠️ 折数不是越多越好 |
| 多仓用"逐笔满额复利" | 系统性高估（+377% vs +126%） | ✅ 本模块只用现金级真实净值 |
| **★ 训练样本剔除"买不进"的行**（可执行性掩码） | **无效，且在长期限上有害** | ❌ **本模块实测**（V5 + V8，见下） |

**★ 修正：上表里"长周期标签重训"那条的本模块结论是反的。**
旧结论来自**纯 IC 视角**（"IC ≠ Top1 利润"），**没把换手成本算进去**。
本模块口径下，长周期标签是**唯一**能覆盖成本的一条路：成本约 0.15%/次往返，
持有 1 天的策略一年要付 ~35% 的成本，持有 10~20 天降到 1/10~1/20。
⇒ 详见 [LESSONS.md](LESSONS.md) §8.1 **R1**（该条是"决定盈亏的第一变量是期限"的定论）。

#### 死胡同详录：可执行性掩码（2026-09-18 本模块实测）

**做法**：把「T 日停牌」与「T 日收盘涨停」的行从**训练/验证样本**里切掉
（判据 PIT 安全，精确率 99.6%，见 [LESSONS.md](LESSONS.md) §8.4），测试集不动。

**两次独立测量，结论一致**：

| 实验 | 对照 | 1d | 3d | 5d | 10d | 20d |
|:--|:--|--:|--:|--:|--:|--:|
| **V5**（LightGBM） | V3 同配方不带掩码 | +0.002 | +0.005 | +0.005 | +0.002 | +0.001 |
| **V8**（XGBoost） | 同折同种子，只差掩码 | **+0.021** | −0.006 | −0.010 | −0.014 | **−0.018** |

- V5 的 Δ 全部在折间跨度（±0.03~0.09）以内 ⇒ **未观察到效应**。
- V8 的 Δ 是**同折同种子**的干净对照，方向一致地为负（除 1d）⇒ **在长期限上明确有害**。
- 教训在 [TODO.md](TODO.md) §12 置顶：**两折的差值不构成结论**（V5 折 1/2 时曾看起来"明确有效"，四折后消失）。

**为什么会有害（机制推测，待专门验证）**：把涨停行从训练里拿掉之后，模型对这批票的打分
变成**没学过**的外推值；而测试集是**全截面**的，它们照样被打分、照样可能排到前面。
于是"训练时看不见、打分时乱给分"——比让它看着学更糟。

**★ 这个"死胡同"里还留着一条活路（记进 [TODO.md](TODO.md) §12）**：如果要在训练侧剔，就必须在**打分侧也剔**
（把 T 日涨停/停牌的票直接置 NaN）。而现在的实现只剔了训练侧。
不过要提醒：**执行层的 `entry_ok` 已经在用 T+1 的**真实**一字板状态过滤候选**（信息更准），
所以"打分侧再剔一次"的增量价值可能同样很小 —— 真要做得先算清楚它比 `entry_ok` 多拦了什么。

---

## 11. 坑清单（都踩过）

1. **`read A B C <<< "..."` 会把多出来的字段全塞给 C** —— 折列表被截成 `"1"`、并发算错。
   改用 `mapfile -t` 按行读。
2. **按年分区的产物路径不要带 `.parquet` 后缀** —— 写的时候被 `with_suffix("")` 剥掉、
   读的时候 `.exists()` 永远 False。
3. **中文表头对不齐**：`str.format` 按字符数算宽度，中文占 2 列 ⇒ 用 `evaluate.pad()`。
4. **`os.cpu_count()` 报宿主核数**（192 vs 容器 32）；**`OMP_NUM_THREADS` 容器预置了 32**，
   `setdefault` 会被顶掉 ⇒ `main.py` 里**硬赋值**。
5. **numpy 链式布尔索引赋值 `arr[m1][m2] = v` 静默丢弃写入** ⇒ 用 `np.flatnonzero(m1 & m2)`。
6. **"文件已存在即跳过"的 resume 会掩盖重构回归** ⇒ 改脚本后首跑必须 `--clean`。
7. **训练 wrapper 用 `nohup` 且绝不加 timeout**（SIGTERM 会传染杀掉子进程）。
8. **不要用 `pkill -f`**（会匹配到自己那条命令行并自杀）⇒
   `ps -eo pid,args | grep '[m]ain\.py'` 找 PID 再 kill。
9. **`/root` 是 0700**：一律用 `/autodl-fs/data/...` 真实路径。
10. **LightGBM 的 `predict` 对 0 行会抛 `ValueError`** ⇒ 无 test 的单元必须显式跳过空块。
11. **LightGBM 4.x 把 `fobj` 从 `train()` 的参数表里拿掉了** —— 自定义目标现在走
    `params["objective"] = 可调用对象`（实测 4.7.0 传 `fobj=` 直接 TypeError）。
12. **按日归约的损失，求和范围写错是"单日对、多日静默错"** ——
    `∂L_top/∂s_j = −(1/τ)·p_j·(z_j − S_d)` 里的 `S_d` 必须是**当日截面内**的和。
    写成全数组求和时，单日用例逐位正确、多日用例悄悄错掉（softmax 是逐日归一的，
    跨日求和等于把各天 z 的均值混在一起）。⇒ `tests/test_labels_losses.py` 用多日夹具 +
    数值梯度对拍来钉死这条。
13. **类型映射表查不到就"静默退回默认值"是重灾区** —— `preprocess.fill_mode_for` 原本是
    `{"gbdt":..., "linear":...}.get(mtype, "lgbm")`，新增 `linear_w` 之后查不到 ⇒ 悄悄退回
    "保留 NaN" ⇒ 加权岭拿到带 NaN 的矩阵直接崩。现在改成**从模型类自己的 `fill_mode` 属性取**，
    查不到类型就**报错**，不留静默默认值。
14. **日志说错话比不打印更危险** —— `train_one` 曾把"模型参数"原样喂给 `describe()`，
    而它去找的是摊平后的键（`transform` / `half_life`），两个都读不到 ⇒ 日志谎报成
    "V2 口径（等权）"，而模型其实在用秩高斯 + 衰减权重。归因时会把结论安到错误的配方上。
    ⇒ `labels.loss_spec()` 把口径**摊平成唯一一种形状**，全流程共用。
15. **`to_numpy(copy=False)` 可能返回只读视图** ⇒ `np.clip(X, lo, hi, out=X)` 直接
    `ValueError: output array is read-only`。载入时夹取要用不带 `out` 的 `np.clip`（新分配一次）。
16. **测试夹具必须复刻真实路径** —— 直接把带 NaN 的原始 X 喂给 Ridge 会报 "Input X contains NaN"，
    那不是引擎的 bug，是夹具绕开了 `preprocess.fill_for`。同理：夹具造标签时要**先用干净特征算信号、
    再注缺失**，否则标签里跟着出现 NaN，神经网络会训出全 NaN 的预测。
17. **测试用的轮数要匹配模型族** —— 神经网络 2 个 epoch 是真的学不动（实测 corr 只 +0.09，
    30 个 epoch 才到 +0.29）。用 2 个 epoch 去断言"预测与标签正相关"，测的是"我配的轮数太少"。
18. **★ 排序目标下 `sample_weight` 必须按"组"给，不能按"行"** —— `XGBRanker.fit` 里的
    `sample_weight` 语义是"**每个 query group 一个权重**"。把按行的权重直接传进去，
    XGBoost 报的是
    `Check failed: group_weights.size() == group_ptr.size() - 1 (3108185 vs. 1106)`
    —— 两个数分别是**行数**和**交易日数**，错误信息完全不指向根因（V4 首跑整折崩在这里）。
    修法见 `mx/models/xgb.py:_group_weights`：按日取组内均值。
    ★ 之所以均值就够：本模块给排序头的权重只有时间衰减，它**同一天内是常数**，
      组内均值 = 每行原值。若将来给排序头加上日内变化的权重（如尾部加权），
      **这个归约方式需要重新定义**，不能沿用。
★ **更多坑（只在 [LESSONS.md](LESSONS.md) §8.6 有记录）**：
"净值与日期差一天"（同族的静默错位）、`engine_hash` 漂移与"不重跑判据"、
`${#FOLDS_LIST}` 是字符数不是折数、以及"**数据重建的等价性是巧合不是保证**"这条认识论结论。

19. **三个头用了同一个模型名 ⇒ 产物互相覆盖，且不报错** —— 产物路径是
    `artifacts/fold{k}/<头名>__<标签>/`，而头名 = `build(cfg, name)` 的 name。
    V6 首跑写了三个 `build(cfg, "nn_top", …)`，结果只剩最后一个的产物，**前面两个静默消失**。
    修法：在 `conf.yaml` 里加等价的 `type` 别名（`nn_mse` / `nn_ic`）。

---

20. **★ 函数内定义的 `nn.Module` 不可 pickle —— 训练跑完、落盘那一刻才炸**（2026-09-19 复刻
    `nn_v8` 时实测）。把网络类写进 `fit()`/`_build()` 里，`pickle.dump(self)` 会报
    `Can't pickle local object ...<_build.<locals>._Net>`，而**前面几十分钟的训练全部白跑**。
    ⇒ 网络类一律建在**模块级**。`tests/test_models.py` 已加"每个头 save/load 往返"这一条，
    秒级就能钉死这类错误（这类 bug 训练时完全看不出来）。
21. **新增"逐日统计量"的打分头，必须同时改**三处调用点 —— `mx/train.py`（评价块 / 全面板落盘）
    与 `mx/predict.py`（审计截断复算）。漏掉任何一处都**不报错**，只是把整块面板当成一天去
    标准化，打分排名整体走样。V13 的 `nn_v8` 就是这么落地的（`predict_needs_day` 开关）。
22. **闭式解热启动不要整块 `astype(float64)`** —— 250 万行 × 305 列转一次 float64 就是
    **6 GB 的额外副本**，两折并发足以把容器顶穿。分块累加 `XᵀX`，峰值只与块大小有关。
23. **★ 新增神经网络类型必须同时登记进 `--smoke` 的压缩清单**（`mx/train.py:170`）——
    那里是 `if m.type in (...)` 的**白名单**，新类型不在里面 ⇒ `--smoke` **按全量跑**、
    与正式训练同价（2026-09-19 复刻 `nn_v8`/`nn_wpcc` 时实测：冒烟跑了整整 6 分钟才做完
    一个 (头×标签)，而它本该是秒级）。★ 同理：任何"按类型分派"的白名单都要检查一遍
    （`preprocess.fill_mode_for` 当年就是这么翻的车，见 §11-13）。
24. **★★★ 一个把致命错误降级成警告的 `try/except`，比直接崩掉危险得多。**
    2026-09-20 抓到：`mx/prices.py` 调 `read_price_frame(root, years=...)`，而那个函数
    **从来没有 `years` 参数** ⇒ 每次回测都抛 `TypeError`。但 `analyze` 把它接住了、
    降级成"不套可买掩码"继续跑，只打一行 ⚠️。于是**回测路径自诞生起就没真正跑通过**，
    而谁也没发现 —— 上一轮还把它归因成"P 块残缺"，其实 P 块补全了它照样跑不了。
    ★ 崩掉你会去修；降级了你会以为它在工作。**写兜底前先问：这个错被兜住之后，
      功能还剩什么？如果答案是"看起来还在跑但结论无效"，那就该让它崩。**
25. **`run.py`/`analysis.py` 里的 `sys.path` 指向哪，决定了它用什么引擎。**
    写成 `parents[1]`（模块根）会命中**另一份**引擎（见 [SPEC.md](SPEC.md) §0「独立单元」末尾那段）。
    这类错**在"顶层恰好没有 mx/"时表现正常** —— 换个环境就翻车。写 `parents[0]`。
26. **★★ "配方声称的特征集"与"实际训练用的列"会分家 —— 而且产物看起来完全正常。**
    2026-09-21 做 RD6 因子筛选时实测：`RECIPE['features']` 换成了 302 列的显式清单，
    但 `model.py::main()` 里**训练入口那一行**仍是写死的 `Panel('all')` ⇒ **实际拿 337 列训练的**。
    `run_id` 照常由"新配方"算出、`complete.json` 照常写、报告照常出 —— 只有 `--audit` 的
    重载对拍在**最后一步**炸出来：`size mismatch ... [256, 337] ... [256, 302]`。
    ⇒ ★ **这道对拍值一次完整的训练时长**：没有它，V27 会以"302 特征模型"的名义
    发出一份 337 特征的产物，而且**谁也不会发现**（这正是 §11-24 那条"崩掉你会去修、
    降级了你会以为它在工作"的同族）。
    ⇒ 纪律：**凡是有"配方里声明一个集合 / 路径 / 口径"的地方，入口处都要问一句
    "这里读的是配方的那个值，还是另写了一遍？"** —— 写第二遍就会漂。
    同类先例：`--out` 只重定向一半（R21-①）、`fill_mode_for` 的静默默认值（§11-13）、
    `--split` 漏传 folds（本轮 V24 也踩到一次，只是它不影响产物）。


## 2026-09-21 因子研究优先：V53–V55（预登记，尚无收益结论）

用户要求重点研究不同时期适用因子。新配方每季每折只读取训练集最近504日，将原337列按每日Pearson IC绝对均值减去四个时间段均值标准差的0.5倍排序，固定保留128列；常数列IC记0，精确同分按原列序。选完后本季度不再变化。保留337维网络、权重初始化和全部训练参数，使用存入每轮checkpoint的0/1输入门控，原337列覆盖率不变。对照V32/V34/V36，同3253/3254/3255种子，全部三组及全部组合报告。不是全样本标准化，不读取验证/测试标签；四折训练日期不同，最近504训练日不能概称最近两个自然年。CPU/GPU验证已通过验证/测试数据扰动不影响选择、无效列不影响推理、checkpoint门控重载及原始初始化逐位一致。评价仍是已反复查看的固定窗口。

队列调整：在运行的V50–V52复核完成后，V53–V55 → V44–V46 → V47–V49 → V41–V43；仅重排等待中的协调进程，重训练仍串行，84GB提前终止阈值和90GB用户上限不变。


## 2026-09-21 V50–V52初始化复核完成：稳定性筛查失败

原配方三个互不重叠新基础种子15361/28403/49117全部完成，28账户独立流水审计通过。新三模型10万元组合：每日换手10bp净收益−8.95%，市场门槛10bp净收益+27.96%、最大回撤19.51%、后半程+21.59%，但剔最好5日−18.04%。全部三个留一组合剔最好5日也为负（−24.43%、−9.12%、−24.44%）。因此事前登记的阶段筛查失败；保留全部结果，禁止改选种子/子集来修补。旧组10bp门槛组合+92.59%不能被解释为已验证的稳定盈利能力。新组3bp门槛组合+53.33%，剔最好5日仍−2.34%。当前优先队列已接续V53–V55训练内季度因子筛选，后接市场状态/事件输入，尚无因子方向收益结论。

新增固定配对诊断：每个方法与对应原种子账户按同日比较，完整28种账户；报告各季度收益差，20日连续区块/2000次/rng119，季度归属已复现全年/H1/H2。属于同窗描述性时间敏感度，未校正多次研究偏差，不作为未来盈利概率。


## 2026-09-21 V53季度筛选首组完成（1/3，禁止整体定论）

全部16个季度模型通过流水线审计；190份checkpoint门控逐位核查，实际128有效因子，峰值总内存73.812GB。Top5每日3bp +10.35% vs基线+14.79%，Top1每日−24.85% vs+123.35%；Top5门槛10bp −1.64% vs+26.00%，尚无主比较改善。V54已按既定配方接续，V55随后。

同时保留全部27种主流水线已有策略的配对，不新扫参数：首组Top5每5日换仓相对同策略基线提高41.99pp，但后半程仍−8.84%；某些排名退出组合也改善。不能因日换仓变差而漏看因子与持有期的交互，也不能选单行最高值当稳定结论。此表是事后完整探索，Top5每日主比较不改变，必须继续检查三种子、成本与时期稳定性。相关脚本V32/family_strategy_pairs.py已接入选择/状态/事件/一致性汇总。


## 2026-09-21 两组初始化的完整策略成本检查（已登记，待完整执行）

只使用model既有打分与27种已有策略，比较V32/V34/V36和V50/V51/V52两组原配方初始化，单边滑点固定3/10bp。各版本仍各自10万元账户，不能将账户均值解释为10万元资金组合。每种策略报告所有六个模型、组内最差/中位/最高、后半程、最差季度和剔最好5日；不同种子之间不伪装成同种子配对。仅描述性检查每组三个成员的净收益、后半程、剔5日是否全部为正，不据此自动晋级。

登记在六版主报告及原有每日/市场门槛复核结果已知之后，因此是同窗完整探索，不能称为未见盲测。没有新增策略参数或替换种子。V32/replication_strategy_costs/plan.json保存2026-09-21T08:02:39.205573+00:00登记时间。324个成本账户将全部逐日现金/持仓独立复算；3bp应精确复现主报告。现金复算小样本检查、异常账目拒绝检查以及组内负成员/缺失成员/重复成员检查已通过，不能代替真实账户验证。

程序已接入V55汇总，在三组筛选成本检查后、V41一致性训练前串行执行。复用旧组账户必须核对打分来源、脚本和产物哈希及54份账户审计。没有新增因子或输入列。


## 2026-09-21 固定3轮训练：V56–V61预登记

仅限model，输入保持冻结337列。读取原配方六种初始化共96个已完成模型的训练/验证历史：训练轮数7/9/14（最少/中位/最多）；74/96训练损失下降但验证IC由首轮到末轮下降；最佳val_wei在前三轮60/96，最佳验证IC在前三轮62/96，验证分数逐轮波动的收益项绝对变动份额中位98.05%。这些是描述性训练证据，不读取测试收益来选轮，也不能据此证明缩短训练有收益。原始历史SHA及全表见V32/training_stability/。

新版本V56/V57/V58分别配对V32/V34/V36（3253/3254/3255），V59/V60/V61配对V50/V51/V52（15361/28403/49117）。唯一配方变化为max_epochs=3；仍平均所有已训练轮次。每个版本独立重训、每个季度/折前三轮权重与原模型逐位核查，不复写旧checkpoint指纹。保留六个固定初始化，不扫描轮数、不换种子。完整流水线、Top5每日主比较、Top1、全部27种已有3bp策略和Top1/Top5的每日/门槛3/6/10bp压力均保留。两个固定三模型资金组合及各自所有留一组合分开报告，比较对应种子的原配方账户。

注册与源码清单见V56/short_training_research/plan.json，组合方案见V32/portfolio_families/short_original和short_replication的plan.json。队列在V41–V43全部完成后接续，训练/完整分析仍串行；没有新增因子或输入列，没有修改评价窗，没有自动晋级。新版本当前只处于验证/排队阶段，暂无收益结论。


## 2026-09-21 V53–V55与完整成本复核完成：不采用筛选方案

现有337列训练内筛选128列的三组实验全部完成。Top5每日3bp新净收益+10.35%/+14.95%/+13.44%，相对同种子基线分别−4.45/−7.76/−2.72pp，主比较0/3改善。Top5门槛10bp只有1/3改善，三个版本剔最好5日均为负。固定三模型真实10万元组合：门槛10bp+0.93%、回撤18.62%、后半程−6.27%、剔5日−25.97%；每日10bp−34.28%、回撤53.41%。全部28个组合/单模型账户独立流水审计通过，但没有稳定收益改善。保持原337列作为后续训练对照，不采用本版筛选。

完整27种已有策略的3/10bp成本复核也已结束。筛选版某些低换手规则相对基线3/3改善，但27种规则在10bp下均至少有一个初始化的后半程和剔5日为负，不能据全年增幅认定稳健。原配方旧、新两组共六个初始化的27种策略全部保留，各组均没有一种规则通过“组三个成员净收益、H2、剔5日全部为正”的描述性筛查。本检查不含额外市场门槛，不能外推为所有策略均无效。两份成本报告各324账户，重用旧对照162账户后共486个不同版本/策略/成本账户，均独立现金持仓复算，3bp复现主报告。相关完整表见V32/strategy_cost_full/与V32/replication_strategy_costs/。

一致性训练V41已接续；V56–V61固定3轮实验六版CPU/CUDA、初始权重/推理一致性和22个类/函数静态同一性检查通过，协调PID147485在等待一致性队列，未并行启动重训练。


## 2026-09-21 已有市场门槛 × 全部持有规则（登记后待执行）

只做模型侧交易策略组合，不新增因子、输入列或均线参数。沿用已有60日等权市场门槛，与全部27种既有持有/退出规则结合，完整保留V32/V34/V36和V50/V51/V52两组三个固定初始化、3/10bp，324个实际单模型10万元账户。原始未加门槛账户已经审计，逐项对照；不把单模型均值当成资金组合。

明确执行语义：T收盘的风险关闭信号覆盖旧目标及最低持有天数，于T+1开盘尝试清仓，仍遵守T+1与停牌/跌停拒单；周期策略重新入场等待原定调仓日。简单屏蔽当日分数不足以实现该语义：周期策略可能沿用旧目标，排名退出也可能保留未满持有期仓位。本次新增研究引擎gated_holding_engine.py，旧引擎和模型版本保持不变。原始现金流水逻辑不改，只在执行前覆盖目标。

54个风险全开启测试账户精确复现旧引擎，54个全关闭账户全现金；周期中间关门、覆盖最低持有天数、受阻卖出、未来风险信号扰动不改变过去、错误轴/类型拒绝检查均通过。真实账户仍须逐日独立复算，所有买入必须有前日risk_on授权，关门后残留持仓只能是卖出受阻；Top1/Top5日换仓还必须复现原门槛成本报告。

注册时点2026-09-21T08:28:52.830723+00:00，已有六版主报告、未加门槛的27种策略以及Top1/Top5门槛结果都已知，因此是同窗组合探索，不是未见盲测。V32/gated_holding_research/plan.json保留全部规则与源码来源，已接入V41汇总，训练与完整分析仍串行；没有自动晋级。


## 2026-09-21 市场门槛 × 27种持有规则完成；V41首组完成

固定门槛与所有27种已有规则、两组共六个原配方初始化的324个真实账户完成，现金/持仓及风险关闭执行核验通过，Top1/Top5每日精确复现既有门槛报告。10bp下两组都没有规则满足组三个成员净收益/H2/剔5日全部为正，不自动采用或晋级。

存在需继续检查的权衡：Top5每20日换仓＋原市场门槛，在六个初始化下全年净收益均为正（+7.40%至+48.08%，中位+18.83%），最差最大回撤15.19%；但最差H2−5.39%、最差剔最好5日−12.43%，没有通过稳定性筛查。另一些低换手排名退出规则全年也6/6为正，但旧组最大回撤仍可达约32%，不能只报新组三个较好结果。全部表见V32/gated_holding_research/，不是新增未见验证。

20日周期目前锚定评价窗首日，调仓相位尚未验证，可能构成类似种子的彩票效应。后续若推进这一候选，须保留完整评价窗、全部六个初始化和所有20个相位，不择优选相位，不把多个相位满额账户均值当真实五仓组合。此前批次没有扫描该参数，也不能将此候选解释为已稳定。

V41双随机前向一致性首组16模型完整流水线通过：Top5每日3bp+0.38% vs原V32+14.79%，后半程−33.19%；Top5门槛10bp+12.78% vs原+26.00%，H2−12.92%、剔5日−7.78%。Top1门槛10bp+71.61%、H2+0.46%、剔5日+7.03%，是次要单初始化结果，不替换主比较。V41守护峰值76.443GB，exit0；V42已按原设置接续，V43与六版固定3轮实验继续排队。


## 2026-09-21 Top5/20日门槛策略：全相位检查已登记

V32/phase_holding_research/plan.json登记时间2026-09-21T08:46:53.973101+00:00。选中这一候选是在27种规则与六个相位0账户结果已知之后，故是事后稳健性检查，不是未见验证。固定V32/V34/V36/V50/V51/V52、Top5、20交易日周期、原60日风险门槛、3/10bp，全部相位0至19，合计240账户；不挑最好相位、不换种子。

每个相位均从官方同一评价起点以10万元现金开始，保留完整242个信号日和相同结算边界；首次计划调仓前持有现金，风险关闭不重置相位，重入等待原计划。这样保持窗口一致，检查冷启动日期敏感性；不声称覆盖有历史持仓的稳态账户。相位和初始化高度依赖，分布不是独立样本概率，也不把各满额账户平均视为符合五仓限制的真实组合。

独立phase_holding_engine.py保留原门槛引擎，旧引擎未改。两档滑点的全部20相位小样本测试通过：同一日期边界、首次现金、买入日期、独立流水、相位0原引擎精确复现、全关全现金、关门与卖出受阻不重置相位、未来信号扰动不改过去、非法相位拒绝。真实账户阶段还将核对相位0与已完成候选的逐日净值，并复算所有账户资金及费用。

已接入V42汇总，在现有门槛研究校验之后串行执行。这里只改策略执行日程，不涉及新因子或模型输入。结果尚未产生，未据此晋级。


<!-- PHASE_AUDIT_COMPLETE_20260921 -->
## 2026-09-21 全相位复核完成、V42完成

全部240个账户流水和执行检查通过，最大现金/权益复算误差0.00000000元。10bp下六个模型各自盈利相位仅8–13/20，120个模型×相位账户中60个盈利；这些账户高度相关，比例不是未来盈利概率。每个模型均有亏损相位，全相位最差收益-19.15%、最差回撤-35.25%；六模型同时全年盈利的相位仅4/20，同时通过全年、后半程、剔除最好5日全正筛查的相位为0/20。原相位0六模型全年均正的结果对起始日期敏感，不采用为稳定收益升级，不挑选新相位替换。所有相位保持完整242个信号日、同一结算终点及10万元现金冷启动；这是事后稳健性检查，不是新时间外验证。

V42完整流水线与压力流水审计完成。Top5每日3bp净收益+8.55%，基线+22.71%，差-14.16个百分点；门槛10bp净收益+12.58%，后半程-4.07%，剔最好5日-8.03%。一致性已完成两组主比较均下降，V43继续，随后V56–V61固定三轮实验。V42守护峰值77.21GB，退出码0；90GB上限及84GB提前停止阈值不变。

明细：`V32/phase_holding_research/REPORT.md`；一致性配对：`V41/consistency_research/REPORT.md`。


<!-- CONSISTENCY_FINAL_20260921 -->
## 一致性训练最终结论

V41–V43全部完成。Top5每日3bp主比较三组均下降，平均-15.44个百分点；固定门槛10bp三组收益也均低于对应基线，后半程及剔最好5日均为负。

完整三模型实际资金组合门槛10bp净收益+62.93%，基线+92.59%，相差-29.65个百分点；最大回撤-16.85%、后半程+8.05%、剔最好5日+2.38%。三种留一组合中，1种剔5日不为正，最差为-0.82%。相对财富变化-15.40%，固定区块诊断区间[-40.15%, +14.53%]，不能作为未来盈利概率。

28个实际资金账户流水审计全部通过。训练流水线最高总内存77.45GB，低于90GB上限。不采用为稳定收益升级；没有分离双前向平均与一致性正则各自贡献，不从该结果推断任一单独机制必然无效。按用户收尾指令不追加消融或新方向，只完成已登记V56–V61，补齐结论后暂停。


<!-- WIND_DOWN_DELIVERY_CORRECTIONS_20260921 -->
## 收尾交付核查与报告文案勘误

收尾状态归入 V56/logs/research_wind_down.json；最终核查产物写入 V56/logs/research_wind_down_audit.json；汇总置于 V56/short_training_research/WRAPUP.md，保持 README 目录四份规程文档。最终检查覆盖注册源码哈希、16季度折、1d榜单数值、四列float32打分产物、最新推理表、费用对照、权重前缀及组合流水。

固定三轮版本的注册 analysis.py 报告模板存在一处“最多30轮”的旧文案，实际锁定配方和检查点均为3轮。为保留注册源码字节，V56/correct_training_report_text.py 在汇总时只校正生成的 REPORT.md 文案，并记录修正前后哈希及收益表未改变的哈希；记录见 V56/short_training_research/report_text_corrections.json。若单独用原注册模板重新生成报告，随后运行该校正脚本。此项不改变训练、预测或数值结果，不追加实验。


## 固定三轮训练最终结论

已登记 V56–V61 六组、96 个季度折模型全部完成。输入、初始化、网络和优化器保持原注册设置，仅把最多30轮改为固定3轮并平均全部3轮打分；288个检查点与对应原模型前三轮权重逐张量完全一致。

Top5每日换手、单边3bp主比较：收益改善 1/6，平均收益差 -7.16 个百分点。全部六组的结果如下，保留改善和下降。

| 配对 | 基线收益 | 三轮收益 | 差值（百分点） | 三轮后半程 | 三轮剔最好5日 |
|---|---|---|---|---|---|
| V32→V56 | +14.79% | +7.47% | -7.32 | -17.96% | -16.58% |
| V34→V57 | +22.71% | +1.71% | -21.01 | -28.12% | -19.29% |
| V36→V58 | +16.16% | +9.11% | -7.05 | -14.22% | -14.25% |
| V50→V59 | +7.53% | +3.58% | -3.95 | -13.51% | -18.38% |
| V51→V60 | -3.32% | +11.95% | +15.27 | -16.64% | -12.54% |
| V52→V61 | +8.64% | -10.29% | -18.93 | -24.94% | -28.73% |

Top5固定市场门槛、单边10bp：收益改善 2/6；新版后半程为正 2/6，剔最好5日为正 0/6。

实际资金组合按总本金10万元拆成三个独立子账户，重新执行整手、最低佣金、容量和持仓约束；下表为各子账户Top1、固定市场门槛、单边10bp，不能与上面的单模型Top5口径混用。

| 初始化组 | 基线组合收益 | 三轮组合收益 | 最大回撤 | 后半程 | 剔最好5日 |
|---|---|---|---|---|---|
| 原初始化组 V56/V57/V58 | +92.59% | +39.94% | -13.82% | +0.93% | -4.61% |
| 独立初始化组 V59/V60/V61 | +27.96% | +22.41% | -21.46% | +9.39% | -22.41% |

原初始化组 V56/V57/V58：三种留一组合中 1/3 同时通过全年、后半程及剔最好5日收益为正的检查。完整组合相对基线财富变化 -27.34%，固定时间区块诊断区间 [-53.86%, +7.70%]。

独立初始化组 V59/V60/V61：三种留一组合中 0/3 同时通过全年、后半程及剔最好5日收益为正的检查。完整组合相对基线财富变化 -4.33%，固定时间区块诊断区间 [-37.47%, +41.90%]。

结论：不采用固定三轮作为稳定收益升级。局部配对改善予以保留，但不能挑选获益的初始化替代全部配对结论。训练历史中的过拟合迹象不是可执行净收益提升的充分证据；不继续搜索轮次、种子或新训练配方。

两个三模型家族各28个资金账户独立流水审计通过。一致性训练V41–V43也已完成，其28个账户审计通过；最终收尾审计共核验9版本、144个季度折模型、84个组合账户，并检查注册源码、输出格式、榜单、成本和前缀权重。报告模板的旧“最多30轮”文案已通过单独勘误脚本修正，原注册源码及数值结果保持不变。

评价窗为2025-07-01至2026-06-30，同一窗口反复研究且固定股票池有幸存者偏差。种子、折和调仓相位不是独立市场样本；区块诊断区间也不能解释为未来盈利概率。没有将版本晋级best/。

<!-- NEW_FACTOR_ROUND_20260921 -->
## 2026-09-21 新因子快照轮（V62–V74）：两条新规律

> 全文与可复算脚本在 `research/20260921_new_factors/{PLAN,RESULTS}.md`；
> 上游交付依据 `featureengineering/FACTOR_DEVELOPMENT_LOG.md`（两批共 316 个候选因子）。
> 数据：新建快照 `trainingdata_v2`（653 特征，`panel_digest=61bdefef30cbe885`），
> 旧快照 `trainingdata/` **原地保留**当对照/回滚。**旧 337 列在新快照里逐位相同**（9 年逐格 0 处不一致，
> `target`/`prices` 同样），`V62` 对 `V32` 的四折打分**最大逐格差 0.00e+00**、三张评价表逐字节相同。

**R37. 上游新因子在这套配方上不能提高含费净收益：IC 一致变好、钱一致变差。** —— **定论（本数据，六臂 + 三粒种子配对）**
- 同快照、同配方、同 seed 3253，**唯一变量 = 输入列**。3bp 含费、Top1：

  | 臂 | 列数 | 1d IC | Σtop1 | top1 净收益 | 门槛版 | 门槛剔最好5日 |
  |:--|--:|--:|--:|--:|--:|--:|
  | V62 旧 337 列 | 337 | +0.0378 | +1.208 | **+123.35%** | **+160.57%** | +61.89% |
  | V63 全部 653 列 | 653 | **+0.0403** | +0.093 | −23.65% | −5.23% | −40.05% |
  | V64 +mfx28+efx32 | 397 | +0.0394 | +0.714 | +37.66% | +88.57% | +21.52% |
  | V65 +按 |IC| 前 34 | 371 | +0.0390 | +0.388 | −2.98% | +34.51% | −20.06% |
  | V66 +随机 40 | 377 | +0.0398 | +1.617 | +230.20% | +272.91% | +117.82% |

- **不是成本解释**：V63 交易 312 笔 / 费用 15,021 元，都比对照（322 笔 / 28,927 元）**少**。
- **机制读数（名次剖面，已套次日开盘可买掩码）**：对照第 1 名 **+0.4991%/天（t=2.23，剖面最高）**；
  653 列版第 1 名 **+0.0384%/天（t=0.20，剖面最高在第 6 名）**。两者第 1 名只在 **27.7%** 的日子相同，
  分歧日对照的第 1 名仍 +0.490%、653 列版的只有 −0.147%
  ⇒ **多加的列把头部换成了另一个（无用的）选择**，而不是把好票弄丢。
- **★ V66 的 +272.91% 是抽签，不采用**：同列清单换三粒互不重叠的基础种子 →
  seed 3253 **+112.3pp**、3254 **+9.5pp**、3255 **−110.2pp**；均值持平（+3.9pp）、
  最差情形更差、剔除最好 5 日的中位数从 +57.59% 掉到 +19.50%。
- **跨特征集打分混合也无效**：V62⊕V63 的权重扫描里 **IC 单调升、收益单调降**
  （门槛版 +160.57%→+187.28%(w=.25)→+101.40%→+34.67%→−5.23%），且 w=.25 那点的回撤 −23.74%
  远差于 w=0 的 −12.40% ⇒ 不构成采用依据。⇒ **R30 的边界补一条：跨特征集的"加"是拖拽，不是增强。**
- **诊断结论**：|IC|>0.015 的 34 个新因子**全部**来自 `mfx_`(28) 与 `efx_`(32)，
  `afx_`（244 个财务报表扩展）与 `qf_`（12 个）**一个都没有**，且它们与旧列的 |秩相关| 多在 0.15~0.52
  ⇒ **信息是真的，这个网络吃不下**。要用它们必须换机制（独立残差 / 两支路），那需要新的预登记。
- ⚠️ **方法论**：V64/V65/V66 三臂只差"加了哪几十列"，结果却相差 190pp 以上
  ⇒ **单种子读数在 ±80pp 量级上不可解读**，任何"某改动有效"都必须走三粒种子配对。

**R38. 种子的「打分层面」集成：**买到的是稳定性，不是收益** —— 均值基本持平、最差情形单调改善。** —— **定论（本数据，9 粒种子枚举全部 511 个子集）**
- 做法（`research/20260921_new_factors/seed_ensemble_size.py`）：把每个种子自己的**四折打分先相加**，
  再对 k 个种子的集成分**逐日按截面标准差对齐后线性相加**（线性缩放不改当天排名 ⇒ k=1 逐位复现该版本自己的数）。
- 9 粒种子 = 3253/3254/3255（V62/V34/V36）+ 15361/28403/49117（V50/V51/V52）+ 3301/3302/3303（V72/V73/V74）：

  | k | 组合数 | 门槛净收益**均值** | **最差** | 最好 | 回撤均值 |
  |--:|--:|--:|--:|--:|--:|
  | 1 | 9 | +85.36% | **+10.21%** | +160.57% | −38.74% |
  | 2 | 36 | +77.33% | −9.99% | +188.56% | −38.07% |
  | 3 | 84 | +91.30% | −20.85% | +218.27% | −38.61% |
  | 4 | 126 | +91.87% | −8.60% | +203.96% | −38.96% |
  | 5 | 126 | +96.08% | +16.70% | +216.23% | −38.95% |
  | 6 | 84 | +89.50% | +23.86% | +161.45% | −39.26% |
  | 7 | 36 | +85.65% | +46.72% | +154.95% | −39.40% |
  | 8 | 9 | +82.84% | +63.38% | +107.56% | −38.92% |
  | **9（全取）** | 1 | **+80.55%** | **+80.55%** | +80.55% | −40.35% |

- **★ 均值不是单调的**（k=2 的 +77.33% 低于 k=1 的 +85.36%），峰值在 k=5（+96.08%，比 k=1 高 10.7pp）；
  **不要把它读成"越多越好"**。★ 早先用 6 粒种子算出的"k=1→5 单调"是**子集构成的运气** ——
  同一批种子扩到 9 粒后那条单调性就消失了。**这是"小样本上看曲线"的又一次教训。**
- **★ 单调的是"最差情形"**（k=3 起）：−20.85% → −8.60% → +16.70% → +23.86% → +46.72% → +63.38% → **+80.55%**。
  即：**k 越大，集成越接近"不会踩到雷"**。单种子是 **+10%~+161%** 的抽签，
  全取 9 粒是**一个确定的 +80.55%**（≈ 单种子均值 +85.36% 与中位 +78.62% 之间）。
- ⇒ **一句话**：**打分层面的种子集成把"单种子的期望"变成"确定的期望"，代价是没有了上限。**
  它不是提高收益的杠杆，是**把收益从抽签变成可预期**的杠杆。
- 数值稳健性：不做尺度对齐、直接相加，三粒度上给 +174.41%（对齐版 +177.72%）⇒ 结论不依赖对齐这一步。
- **验证窗信息不能用来挑种子或加权**（`seed_weighting.py`，8 粒种子）：`val_wei` 跨度只有 1.28~1.40、
  Val RankIC 只有 0.079~0.088，而同一批种子在测试窗上跨度 **150pp**；
  **三项验证指标全都最高的是 V51，而它在测试窗上是最差的那粒（+10.21%）**。
  按 val 指标 softmax(3σ) 加权一律变差（+42.18%/+48.17%/+25.03% vs 等权 +107.56%）。
  ⇒ 与 R29「`val_wei` 里 93% 是 5 只票的噪声」同源 ⇒ **采用等权**。
- ⚠️ **不能与资金层面的三模型组合（+130.67%）互相引用**：那条把 10 万拆成三个子账户、
  各受整手与 1% 容量约束；本曲线是**一个账户吃一份平均打分**。两者都可实施，但口径不同。

**资源与一处工具修正（2026-09-21）**
- 653 列 ⇒ 单折面板 **11.7 GiB**（337 列 6.0 GiB）、16 折墙钟 28.0 min（3 并发，内存闸门自动算出的上限）；
  337 列 13.4 min（4 并发）。全程串行、峰值 anon 47.4 GiB、无 OOM、未超 90 GiB 硬限。
- ★ **修掉一个会误停的内存闸门**：`memory_guard.py` / `analysis.py` 原来拿 `memory.current` 判停，
  而它**包含可回收的 page cache**（初加工读盘会把它顶到 40 GiB+）⇒ 实测在 **anon 只有 41.9 GiB** 时误停
  （current 85.1 GiB，其中 file 43 GiB）。现改为对「current − file」判 84 GiB，anon 仍保留 72 GiB 更严闸门；
  `memory.current` 仍逐行落盘，事后可核对。**硬上限 90 GiB 未动。**
